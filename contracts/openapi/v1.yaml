openapi: 3.1.1
info:
  title: saas-forge Public API
  version: 1.0.0
  description: |
    The versioned public REST contract for saas-forge. This document contains only
    delivery-stage-two and delivery-stage-three resources. Permission, Feature,
    and Quota runtime operations are intentionally absent until their domain
    contracts are reviewed.
servers:
  - url: https://api.example.invalid
    description: Deployment-specific API Gateway origin.
tags:
  - name: Authentication
  - name: Discovery
  - name: Platform tenants
  - name: Platform entitlement bootstrap
  - name: OAuth clients
paths:
  /api/v1/auth/session:
    get:
      tags: [Authentication]
      operationId: getCurrentSession
      x-saasforge-service: iam-service
      summary: Read the current Platform Identity and authorization
      description: |
        Uses only a verified Platform User Access Token. Reads authoritative IAM
        Identity and current PLATFORM_ADMIN authorization, independently of future
        business authorization. Tenant and Service Access Tokens are rejected.
        Does not consume, validate or rotate a Refresh Token Family and does not
        promise that future refresh will succeed. Never sets cookies.
      security: [{ UserBearerAuth: [] }]
      responses:
        '200':
          description: Current request identity and authorization; never cached.
          headers:
            Cache-Control:
              schema: { type: string, const: no-store }
          content:
            application/json:
              schema: { $ref: '#/components/schemas/CurrentSession' }
        '401': { $ref: './common.yaml#/components/responses/Problem' }
        '403': { $ref: './common.yaml#/components/responses/Problem' }
        '405': { $ref: './common.yaml#/components/responses/Problem' }
        '502': { $ref: './common.yaml#/components/responses/Problem' }
        '503': { $ref: './common.yaml#/components/responses/Problem' }
        '504': { $ref: './common.yaml#/components/responses/Problem' }

  /api/v1/auth/context:
    get:
      tags: [Authentication]
      operationId: getCurrentTenantContext
      x-saasforge-service: iam-service
      x-saasforge-java-sdk: true
      summary: Read the current Tenant Context without rotating the browser session
      description: |
        Uses only the verified User Access Token to select the current Membership and Tenant.
        Returns the authoritative Accessible Membership and brand snapshot for this tab.
        Does not accept a client-selected Tenant, consume a Refresh Token or set cookies.
        Platform and Service Access Tokens cannot read a Tenant Context.
      security: [{ UserBearerAuth: [] }]
      responses:
        '200':
          description: Current Tenant Context; never cached.
          headers:
            Cache-Control:
              schema: { type: string, const: no-store }
          content:
            application/json:
              schema: { $ref: '#/components/schemas/TenantAuthenticationContext' }
        '401': { $ref: './common.yaml#/components/responses/Problem' }
        '403': { $ref: './common.yaml#/components/responses/Problem' }
        '409': { $ref: './common.yaml#/components/responses/Problem' }
        '503': { $ref: './common.yaml#/components/responses/Problem' }
        '405': { $ref: './common.yaml#/components/responses/Problem' }
        '502': { $ref: './common.yaml#/components/responses/Problem' }
        '504': { $ref: './common.yaml#/components/responses/Problem' }

  /api/v1/auth/login:
    post:
      tags: [Authentication]
      operationId: login
      x-saasforge-service: iam-service
      summary: Start a browser session with email and password
      description: |
        The request never accepts a Tenant or Membership identifier. A user with one
        accessible Membership receives an Access Token; a user with multiple
        Memberships receives the server-verified selection candidates without an
        Access Token. A Refresh Token is set only as an HttpOnly cookie. The deprecated
        Idempotency-Key parameter remains optional for v1 compatibility and is ignored.
      parameters:
        - name: Idempotency-Key
          in: header
          required: false
          deprecated: true
          description: Retained only for v1 compatibility; login does not use it.
          schema: { $ref: './common.yaml#/components/schemas/UuidV7' }
        - $ref: '#/components/parameters/CsrfHeader'
        - $ref: '#/components/parameters/BrowserOrigin'
        - $ref: '#/components/parameters/FetchSite'
        - $ref: '#/components/parameters/OptionalPlatformRefreshTokenCookie'
        - $ref: '#/components/parameters/OptionalTenantRefreshTokenCookie'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LoginRequest'
      responses:
        '200':
          description: Authentication succeeded.
          headers:
            Set-Cookie:
              $ref: '#/components/headers/RefreshCookie'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuthenticationResult'
        '400': { $ref: './common.yaml#/components/responses/ValidationProblem' }
        '401': { $ref: './common.yaml#/components/responses/Problem' }
        '403': { $ref: './common.yaml#/components/responses/Problem' }
        '409': { $ref: './common.yaml#/components/responses/Problem' }
        '404': { $ref: './common.yaml#/components/responses/Problem' }
        '405': { $ref: './common.yaml#/components/responses/Problem' }
        '502': { $ref: './common.yaml#/components/responses/Problem' }
        '503': { $ref: './common.yaml#/components/responses/Problem' }
        '504': { $ref: './common.yaml#/components/responses/Problem' }

  /api/v1/auth/refresh:
    post:
      tags: [Authentication]
      operationId: refreshAccessToken
      x-saasforge-service: iam-service
      security:
        - PlatformRefreshCookieAuth: []
        - TenantRefreshCookieAuth: []
      summary: Rotate the browser Refresh Token and issue the current Access Token
      description: The Refresh Token is supplied only by the HttpOnly refresh cookie.
      parameters:
        - $ref: '#/components/parameters/IdempotencyKey'
        - $ref: '#/components/parameters/CsrfHeader'
        - $ref: '#/components/parameters/BrowserOrigin'
        - $ref: '#/components/parameters/FetchSite'
        - $ref: '#/components/parameters/OptionalPlatformRefreshTokenCookie'
        - $ref: '#/components/parameters/OptionalTenantRefreshTokenCookie'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SessionSlotRequest'
      responses:
        '200':
          description: The Refresh Token was rotated.
          headers:
            Set-Cookie:
              $ref: '#/components/headers/RefreshCookie'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuthenticationResult'
        '400': { $ref: './common.yaml#/components/responses/ValidationProblem' }
        '401': { $ref: './common.yaml#/components/responses/Problem' }
        '409': { $ref: './common.yaml#/components/responses/Problem' }
        '404': { $ref: './common.yaml#/components/responses/Problem' }
        '405': { $ref: './common.yaml#/components/responses/Problem' }
        '502': { $ref: './common.yaml#/components/responses/Problem' }
        '503': { $ref: './common.yaml#/components/responses/Problem' }
        '504': { $ref: './common.yaml#/components/responses/Problem' }

  /api/v1/auth/password-changes:
    post:
      tags: [Authentication]
      operationId: changeInitialPassword
      x-saasforge-service: iam-service
      security: [{ PlatformRefreshCookieAuth: [] }]
      summary: Replace an initial password and end the restricted session
      description: |
        Accepts only an INITIAL_PASSWORD_CHANGE Refresh Token cookie. A successful
        change revokes the restricted session and requires a new login.
      parameters:
        - $ref: '#/components/parameters/CsrfHeader'
        - $ref: '#/components/parameters/BrowserOrigin'
        - $ref: '#/components/parameters/FetchSite'
        - $ref: '#/components/parameters/PlatformRefreshTokenCookie'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PasswordChangeRequest'
      responses:
        '204':
          description: The password was changed and the restricted session ended.
          headers:
            Set-Cookie:
              $ref: '#/components/headers/ClearedRefreshCookie'
        '400': { $ref: './common.yaml#/components/responses/ValidationProblem' }
        '401': { $ref: './common.yaml#/components/responses/Problem' }
        '404': { $ref: './common.yaml#/components/responses/Problem' }
        '405': { $ref: './common.yaml#/components/responses/Problem' }
        '503': { $ref: './common.yaml#/components/responses/Problem' }

  /api/v1/auth/password-setups:
    post:
      tags: [Authentication]
      operationId: establishPassword
      x-saasforge-service: iam-service
      summary: Establish the first Password Credential with a one-time Challenge
      description: |
        This anonymous endpoint accepts no Identity, Tenant, Membership, or email.
        A successful exchange never creates a login session or sets a cookie.
      parameters:
        - $ref: '#/components/parameters/IdempotencyKey'
        - $ref: '#/components/parameters/CsrfHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PasswordSetupRequest'
      responses:
        '204': { $ref: './common.yaml#/components/responses/NoContent' }
        '400': { $ref: './common.yaml#/components/responses/Problem' }
        '405': { $ref: './common.yaml#/components/responses/Problem' }
        '503': { $ref: './common.yaml#/components/responses/Problem' }

  /api/v1/auth/context-selections:
    post:
      tags: [Authentication]
      operationId: selectAuthenticationContext
      x-saasforge-service: iam-service
      security: [{ TenantRefreshCookieAuth: [] }]
      summary: Complete a pending Tenant context selection
      description: |
        The Identity comes only from the USER_TENANT_SELECTION Refresh Token cookie.
        IAM revalidates the selected Membership with Tenant Access before atomically
        rotating the cookie and issuing a Tenant Access Token.
      parameters:
        - $ref: '#/components/parameters/CsrfHeader'
        - $ref: '#/components/parameters/BrowserOrigin'
        - $ref: '#/components/parameters/FetchSite'
        - $ref: '#/components/parameters/TenantRefreshTokenCookie'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ContextSelectionRequest'
      responses:
        '200':
          description: The pending selection became a Tenant session.
          headers:
            Set-Cookie:
              $ref: '#/components/headers/RefreshCookie'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AccessTokenResult'
        '400': { $ref: './common.yaml#/components/responses/ValidationProblem' }
        '401': { $ref: './common.yaml#/components/responses/Problem' }
        '403': { $ref: './common.yaml#/components/responses/Problem' }
        '405': { $ref: './common.yaml#/components/responses/Problem' }
        '503': { $ref: './common.yaml#/components/responses/Problem' }

  /api/v1/auth/logout:
    post:
      tags: [Authentication]
      operationId: logout
      x-saasforge-service: iam-service
      summary: Revoke the browser Refresh Token
      description: |
        Idempotently ends only the current browser session. The Refresh Token cookie is optional so
        clients can safely retry after the cookie was already cleared. When a valid User bearer token
        is present, IAM also revokes that token's jti for its remaining validity window plus clock skew.
      security:
        - UserBearerAuth: []
        - {}
      parameters:
        - name: Idempotency-Key
          in: header
          required: false
          deprecated: true
          description: Retained only for v1 compatibility; logout does not use it.
          schema: { $ref: './common.yaml#/components/schemas/UuidV7' }
        - $ref: '#/components/parameters/CsrfHeader'
        - $ref: '#/components/parameters/BrowserOrigin'
        - $ref: '#/components/parameters/FetchSite'
        - $ref: '#/components/parameters/OptionalPlatformRefreshTokenCookie'
        - $ref: '#/components/parameters/OptionalTenantRefreshTokenCookie'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SessionSlotRequest'
      responses:
        '204':
          description: The current browser session was logged out.
          headers:
            Set-Cookie:
              $ref: '#/components/headers/ClearedRefreshCookie'
        '400': { $ref: './common.yaml#/components/responses/ValidationProblem' }
        '401': { $ref: './common.yaml#/components/responses/Problem' }
        '409': { $ref: './common.yaml#/components/responses/Problem' }
        '404': { $ref: './common.yaml#/components/responses/Problem' }
        '405': { $ref: './common.yaml#/components/responses/Problem' }
        '502': { $ref: './common.yaml#/components/responses/Problem' }
        '503': { $ref: './common.yaml#/components/responses/Problem' }
        '504': { $ref: './common.yaml#/components/responses/Problem' }

  /api/v1/auth/tenant-switches:
    post:
      tags: [Authentication]
      operationId: switchTenantContext
      x-saasforge-service: iam-service
      security: [{ TenantRefreshCookieAuth: [] }]
      summary: Select or switch the session Membership
      description: |
        Accepts only the target Membership. The host-only Refresh Token cookie is the
        only session locator; a Bearer Access Token is ignored for this purpose. IAM
        verifies the current and target Membership before changing the session. The
        request must originate from a controlled browser Origin and pass Fetch Metadata
        and CSRF checks. Call refresh after 204.
      parameters:
        - $ref: '#/components/parameters/IdempotencyKey'
        - $ref: '#/components/parameters/CsrfHeader'
        - $ref: '#/components/parameters/BrowserOrigin'
        - $ref: '#/components/parameters/FetchSite'
        - $ref: '#/components/parameters/TenantRefreshTokenCookie'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TenantSwitchRequest'
      responses:
        '204': { $ref: './common.yaml#/components/responses/NoContent' }
        '400': { $ref: './common.yaml#/components/responses/ValidationProblem' }
        '401': { $ref: './common.yaml#/components/responses/Problem' }
        '403': { $ref: './common.yaml#/components/responses/Problem' }
        '409': { $ref: './common.yaml#/components/responses/Problem' }
        '503': { $ref: './common.yaml#/components/responses/Problem' }

  /oauth2/token:
    post:
      tags: [Authentication]
      operationId: issueClientCredentialsToken
      x-saasforge-service: iam-service
      x-saasforge-java-sdk: true
      summary: Issue a Client Credentials Access Token
      description: |
        Supports only grant_type=client_credentials. The OAuth Client authenticates
        with HTTP Basic; requested scope must be a subset of its allowedScopes.
      security:
        - OAuthClientBasic: []
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/ClientCredentialsTokenRequest'
      responses:
        '200':
          description: A service Access Token was issued.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ClientCredentialsTokenResponse'
        '400': { $ref: './common.yaml#/components/responses/ValidationProblem' }
        '401': { $ref: './common.yaml#/components/responses/Problem' }
        '403': { $ref: './common.yaml#/components/responses/Problem' }
        '404': { $ref: './common.yaml#/components/responses/Problem' }
        '405': { $ref: './common.yaml#/components/responses/Problem' }
        '502': { $ref: './common.yaml#/components/responses/Problem' }
        '504': { $ref: './common.yaml#/components/responses/Problem' }

  /.well-known/jwks.json:
    get:
      tags: [Discovery]
      operationId: getJwks
      x-saasforge-service: iam-service
      x-saasforge-java-sdk: true
      summary: Get current JWT verification keys
      description: Anonymous RS256 verification-key discovery. Private material and internal signing-key metadata are never published.
      responses:
        '200':
          description: Current public signing keys.
          headers:
            Cache-Control:
              $ref: '#/components/headers/JwksCacheControl'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Jwks'
        '404': { $ref: './common.yaml#/components/responses/Problem' }
        '405': { $ref: './common.yaml#/components/responses/Problem' }
        '502': { $ref: './common.yaml#/components/responses/Problem' }
        '504': { $ref: './common.yaml#/components/responses/Problem' }

  /api/v1/platform/tenants:
    get:
      tags: [Platform tenants]
      operationId: listPlatformTenants
      x-saasforge-service: tenant-access-service
      summary: List Tenants by literal name substring and lifecycle status
      security: [{ UserBearerAuth: [] }]
      parameters:
        - name: cursor
          in: query
          schema: { type: string, maxLength: 2048 }
        - name: limit
          in: query
          schema: { type: integer, minimum: 1, maximum: 100, default: 50 }
        - name: name
          in: query
          schema: { type: string, maxLength: 200 }
        - name: status
          in: query
          schema: { $ref: '#/components/schemas/TenantStatus' }
      responses:
        '200':
          description: Authoritative result; requires current Platform Admin authorization.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/TenantPage' }
        '400': { $ref: './common.yaml#/components/responses/ValidationProblem' }
        '401': { $ref: './common.yaml#/components/responses/Problem' }
        '403': { $ref: './common.yaml#/components/responses/Problem' }
        '404': { $ref: './common.yaml#/components/responses/Problem' }
        '405': { $ref: './common.yaml#/components/responses/Problem' }
        '409': { $ref: './common.yaml#/components/responses/Problem' }
        '502': { $ref: './common.yaml#/components/responses/Problem' }
        '503': { $ref: './common.yaml#/components/responses/Problem' }
        '504': { $ref: './common.yaml#/components/responses/Problem' }
    post:
      tags: [Platform tenants]
      operationId: createPlatformTenant
      x-saasforge-service: tenant-access-service
      x-saasforge-java-sdk: true
      summary: Create a pending Tenant
      description: Requires a Platform Admin user token. The new Tenant is PENDING until administrator initialization succeeds.
      security: [{ UserBearerAuth: [] }]
      parameters:
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateTenantRequest'
      responses:
        '201':
          description: The Tenant was created in PENDING state.
          headers:
            Location: { $ref: './common.yaml#/components/headers/Location' }
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Tenant' }
        '400': { $ref: './common.yaml#/components/responses/ValidationProblem' }
        '401': { $ref: './common.yaml#/components/responses/Problem' }
        '403': { $ref: './common.yaml#/components/responses/Problem' }
        '409': { $ref: './common.yaml#/components/responses/Problem' }
        '404': { $ref: './common.yaml#/components/responses/Problem' }
        '405': { $ref: './common.yaml#/components/responses/Problem' }
        '502': { $ref: './common.yaml#/components/responses/Problem' }
        '504': { $ref: './common.yaml#/components/responses/Problem' }

  /api/v1/platform/tenants/{tenantId}:
    get:
      tags: [Platform tenants]
      operationId: getPlatformTenant
      x-saasforge-service: tenant-access-service
      summary: Read authoritative Tenant detail
      security: [{ UserBearerAuth: [] }]
      parameters:
        - name: tenantId
          in: path
          required: true
          schema: { $ref: './common.yaml#/components/schemas/UuidV7' }
      responses:
        '200':
          description: Authoritative result; requires current Platform Admin authorization.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Tenant' }
        '400': { $ref: './common.yaml#/components/responses/ValidationProblem' }
        '401': { $ref: './common.yaml#/components/responses/Problem' }
        '403': { $ref: './common.yaml#/components/responses/Problem' }
        '404': { $ref: './common.yaml#/components/responses/Problem' }
        '405': { $ref: './common.yaml#/components/responses/Problem' }
        '409': { $ref: './common.yaml#/components/responses/Problem' }
        '502': { $ref: './common.yaml#/components/responses/Problem' }
        '503': { $ref: './common.yaml#/components/responses/Problem' }
        '504': { $ref: './common.yaml#/components/responses/Problem' }

  /api/v1/platform/tenant-creations:
    get:
      tags: [Platform tenants]
      operationId: listTenantCreations
      x-saasforge-service: tenant-access-service
      summary: Find only the current actor creation attempts after losing an in-memory handle
      security: [{ UserBearerAuth: [] }]
      parameters:
        - name: cursor
          in: query
          schema: { type: string, maxLength: 2048 }
        - name: limit
          in: query
          schema: { type: integer, minimum: 1, maximum: 100, default: 50 }
      responses:
        '200':
          description: Authoritative result; requires current Platform Admin authorization.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/TenantCreationPage' }
        '400': { $ref: './common.yaml#/components/responses/ValidationProblem' }
        '401': { $ref: './common.yaml#/components/responses/Problem' }
        '403': { $ref: './common.yaml#/components/responses/Problem' }
        '404': { $ref: './common.yaml#/components/responses/Problem' }
        '405': { $ref: './common.yaml#/components/responses/Problem' }
        '409': { $ref: './common.yaml#/components/responses/Problem' }
        '502': { $ref: './common.yaml#/components/responses/Problem' }
        '503': { $ref: './common.yaml#/components/responses/Problem' }
        '504': { $ref: './common.yaml#/components/responses/Problem' }

  /api/v1/platform/tenant-creations/{creationId}:
    get:
      tags: [Platform tenants]
      operationId: getTenantCreation
      x-saasforge-service: tenant-access-service
      summary: Read own creation result; missing or expired records never authorize a new creation
      security: [{ UserBearerAuth: [] }]
      parameters:
        - name: creationId
          in: path
          required: true
          schema: { $ref: './common.yaml#/components/schemas/UuidV7' }
      responses:
        '200':
          description: Authoritative result; requires current Platform Admin authorization.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/TenantCreationRecovery' }
        '400': { $ref: './common.yaml#/components/responses/ValidationProblem' }
        '401': { $ref: './common.yaml#/components/responses/Problem' }
        '403': { $ref: './common.yaml#/components/responses/Problem' }
        '404': { $ref: './common.yaml#/components/responses/Problem' }
        '405': { $ref: './common.yaml#/components/responses/Problem' }
        '409': { $ref: './common.yaml#/components/responses/Problem' }
        '502': { $ref: './common.yaml#/components/responses/Problem' }
        '503': { $ref: './common.yaml#/components/responses/Problem' }
        '504': { $ref: './common.yaml#/components/responses/Problem' }

  /api/v1/platform/tenant-creations/{creationId}/recovery:
    post:
      tags: [Platform tenants]
      operationId: recoverTenantCreation
      x-saasforge-service: tenant-access-service
      summary: Continue the original creation with its server-stored actor, Key and request; never starts a new operation
      security: [{ UserBearerAuth: [] }]
      parameters:
        - name: creationId
          in: path
          required: true
          schema: { $ref: './common.yaml#/components/schemas/UuidV7' }
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              maxProperties: 0
      responses:
        '200':
          description: Authoritative result; requires current Platform Admin authorization.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/TenantCreationRecovery' }
        '400': { $ref: './common.yaml#/components/responses/ValidationProblem' }
        '401': { $ref: './common.yaml#/components/responses/Problem' }
        '403': { $ref: './common.yaml#/components/responses/Problem' }
        '404': { $ref: './common.yaml#/components/responses/Problem' }
        '405': { $ref: './common.yaml#/components/responses/Problem' }
        '409': { $ref: './common.yaml#/components/responses/Problem' }
        '502': { $ref: './common.yaml#/components/responses/Problem' }
        '503': { $ref: './common.yaml#/components/responses/Problem' }
        '504': { $ref: './common.yaml#/components/responses/Problem' }

  /api/v1/platform/tenants/{tenantId}/administrator-initializations:
    post:
      tags: [Platform tenants]
      operationId: initializeTenantAdministrator
      x-saasforge-service: tenant-access-service
      x-saasforge-java-sdk: true
      summary: Initialize the first Tenant Administrator
      description: |
        Requires a Platform Admin. The Tenant Access workflow verifies or creates the
        Identity, consumes max_users, assigns the administrator role, then moves the
        Tenant from PENDING to ACTIVE. An existing Identity is reused.
      security: [{ UserBearerAuth: [] }]
      parameters:
        - $ref: '#/components/parameters/TenantId'
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/AdministratorInitializationRequest' }
      responses:
        '200':
          description: The Tenant is active and has an initial administrator.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Tenant' }
        '400': { $ref: './common.yaml#/components/responses/ValidationProblem' }
        '401': { $ref: './common.yaml#/components/responses/Problem' }
        '403': { $ref: './common.yaml#/components/responses/Problem' }
        '404': { $ref: './common.yaml#/components/responses/Problem' }
        '409': { $ref: './common.yaml#/components/responses/Problem' }
        '405': { $ref: './common.yaml#/components/responses/Problem' }
        '502': { $ref: './common.yaml#/components/responses/Problem' }
        '504': { $ref: './common.yaml#/components/responses/Problem' }

  /api/v1/platform/tenants/{tenantId}/administrator-password-setups:
    post:
      tags: [Platform tenants]
      operationId: resendTenantAdministratorPasswordSetup
      x-saasforge-service: tenant-access-service
      x-saasforge-java-sdk: true
      summary: Resend the initial Tenant Administrator password setup email
      description: |
        Requires a Platform Admin. Tenant Access resolves the Identity only through
        the immutable initial administrator relationship. No email address, Identity,
        Token, mail content, or delivery state is accepted or returned.
      security: [{ UserBearerAuth: [] }]
      parameters:
        - $ref: '#/components/parameters/TenantId'
        - $ref: '#/components/parameters/IdempotencyKey'
      responses:
        '204':
          description: SMTP accepted the latest setup email, or the Identity already has a valid password.
        '400': { $ref: './common.yaml#/components/responses/ValidationProblem' }
        '401': { $ref: './common.yaml#/components/responses/Problem' }
        '403': { $ref: './common.yaml#/components/responses/Problem' }
        '404': { $ref: './common.yaml#/components/responses/Problem' }
        '409': { $ref: './common.yaml#/components/responses/Problem' }
        '503':
          description: The durable delivery workflow is still pending.
          headers:
            Retry-After:
              required: true
              description: Minimum seconds before polling the same Idempotency-Key again.
              schema: { type: integer, minimum: 1 }
          content:
            application/problem+json:
              schema: { $ref: './common.yaml#/components/schemas/ProblemDetails' }

  /api/v1/platform/tenants/{tenantId}/suspensions:
    post:
      tags: [Platform tenants]
      operationId: suspendTenant
      x-saasforge-service: tenant-access-service
      x-saasforge-java-sdk: true
      summary: Suspend an active Tenant
      description: Requires a Platform Admin. Suspension revokes matching sessions before the Tenant becomes SUSPENDED.
      security: [{ UserBearerAuth: [] }]
      parameters:
        - $ref: '#/components/parameters/TenantId'
        - $ref: '#/components/parameters/IdempotencyKey'
      responses:
        '200':
          description: The Tenant was suspended.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Tenant' }
        '400': { $ref: './common.yaml#/components/responses/ValidationProblem' }
        '401': { $ref: './common.yaml#/components/responses/Problem' }
        '403': { $ref: './common.yaml#/components/responses/Problem' }
        '404': { $ref: './common.yaml#/components/responses/Problem' }
        '409': { $ref: './common.yaml#/components/responses/Problem' }
        '405': { $ref: './common.yaml#/components/responses/Problem' }
        '502': { $ref: './common.yaml#/components/responses/Problem' }
        '503':
          description: The durable suspension workflow is pending (`TENANT_SUSPENSION_PENDING`).
          headers:
            Retry-After:
              required: true
              description: Minimum seconds before retrying with the same Idempotency-Key.
              schema: { type: integer, minimum: 1 }
          content:
            application/problem+json:
              schema: { $ref: './common.yaml#/components/schemas/ProblemDetails' }
        '504': { $ref: './common.yaml#/components/responses/Problem' }
    delete:
      tags: [Platform tenants]
      operationId: resumeTenant
      x-saasforge-service: tenant-access-service
      x-saasforge-java-sdk: true
      summary: Remove the current Tenant suspension
      description: Requires a Platform Admin. Resuming never restores revoked sessions.
      security: [{ UserBearerAuth: [] }]
      parameters:
        - $ref: '#/components/parameters/TenantId'
        - $ref: '#/components/parameters/IdempotencyKey'
      responses:
        '200':
          description: The Tenant was returned to ACTIVE state.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Tenant' }
        '400': { $ref: './common.yaml#/components/responses/ValidationProblem' }
        '401': { $ref: './common.yaml#/components/responses/Problem' }
        '403': { $ref: './common.yaml#/components/responses/Problem' }
        '404': { $ref: './common.yaml#/components/responses/Problem' }
        '409': { $ref: './common.yaml#/components/responses/Problem' }
        '405': { $ref: './common.yaml#/components/responses/Problem' }
        '502': { $ref: './common.yaml#/components/responses/Problem' }
        '503':
          description: The durable resume workflow is pending (`TENANT_RESUME_PENDING`).
          headers:
            Retry-After:
              required: true
              description: Minimum seconds before retrying with the same Idempotency-Key.
              schema: { type: integer, minimum: 1 }
          content:
            application/problem+json:
              schema: { $ref: './common.yaml#/components/schemas/ProblemDetails' }
        '504': { $ref: './common.yaml#/components/responses/Problem' }

  /api/v1/platform/tenants/{tenantId}/suspension-recoveries:
    post:
      tags: [Platform tenants]
      operationId: recoverTenantSuspension
      x-saasforge-service: tenant-access-service
      x-saasforge-java-sdk: true
      summary: Resume an exhausted Tenant suspension workflow
      description: |
        Requires a Platform Admin. Continues the existing fail-closed suspension workflow without
        replacing its IAM revocation request or Revocation Fence generation.
      security: [{ UserBearerAuth: [] }]
      parameters:
        - $ref: '#/components/parameters/TenantId'
        - $ref: '#/components/parameters/IdempotencyKey'
      responses:
        '200':
          description: The original suspension workflow completed and the Tenant is SUSPENDED.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Tenant' }
        '400': { $ref: './common.yaml#/components/responses/ValidationProblem' }
        '401': { $ref: './common.yaml#/components/responses/Problem' }
        '403': { $ref: './common.yaml#/components/responses/Problem' }
        '404': { $ref: './common.yaml#/components/responses/Problem' }
        '409': { $ref: './common.yaml#/components/responses/Problem' }
        '503':
          description: The recovered workflow is still pending (`TENANT_SUSPENSION_RECOVERY_PENDING`).
          headers:
            Retry-After:
              required: true
              description: Minimum seconds before retrying with the same Idempotency-Key.
              schema: { type: integer, minimum: 1 }
          content:
            application/problem+json:
              schema: { $ref: './common.yaml#/components/schemas/ProblemDetails' }

  /api/v1/platform/quota-definition-operations:
    get:
      tags: [Platform entitlement bootstrap]
      operationId: listQuotaDefinitionOperations
      x-saasforge-service: entitlement-service
      summary: Find only the current actor quota definition operation attempts after losing an in-memory handle
      security: [{ UserBearerAuth: [] }]
      parameters:
        - name: cursor
          in: query
          schema: { type: string, maxLength: 2048 }
        - name: limit
          in: query
          schema: { type: integer, minimum: 1, maximum: 100, default: 50 }
      responses:
        '200':
          description: Authoritative result; requires current Platform Admin authorization.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/QuotaDefinitionOperationPage' }
        '400': { $ref: './common.yaml#/components/responses/ValidationProblem' }
        '401': { $ref: './common.yaml#/components/responses/Problem' }
        '403': { $ref: './common.yaml#/components/responses/Problem' }
        '404': { $ref: './common.yaml#/components/responses/Problem' }
        '405': { $ref: './common.yaml#/components/responses/Problem' }
        '409': { $ref: './common.yaml#/components/responses/Problem' }
        '502': { $ref: './common.yaml#/components/responses/Problem' }
        '503': { $ref: './common.yaml#/components/responses/Problem' }
        '504': { $ref: './common.yaml#/components/responses/Problem' }

  /api/v1/platform/quota-definition-operations/{operationId}:
    get:
      tags: [Platform entitlement bootstrap]
      operationId: getQuotaDefinitionOperation
      x-saasforge-service: entitlement-service
      summary: Read own quota definition operation result; missing or expired records never authorize a new quota definition operation
      security: [{ UserBearerAuth: [] }]
      parameters:
        - name: operationId
          in: path
          required: true
          schema: { $ref: './common.yaml#/components/schemas/UuidV7' }
      responses:
        '200':
          description: Authoritative result; requires current Platform Admin authorization.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/QuotaDefinitionOperationRecovery' }
        '400': { $ref: './common.yaml#/components/responses/ValidationProblem' }
        '401': { $ref: './common.yaml#/components/responses/Problem' }
        '403': { $ref: './common.yaml#/components/responses/Problem' }
        '404': { $ref: './common.yaml#/components/responses/Problem' }
        '405': { $ref: './common.yaml#/components/responses/Problem' }
        '409': { $ref: './common.yaml#/components/responses/Problem' }
        '502': { $ref: './common.yaml#/components/responses/Problem' }
        '503': { $ref: './common.yaml#/components/responses/Problem' }
        '504': { $ref: './common.yaml#/components/responses/Problem' }

  /api/v1/platform/quota-definition-operations/{operationId}/recovery:
    post:
      tags: [Platform entitlement bootstrap]
      operationId: recoverQuotaDefinitionOperation
      x-saasforge-service: entitlement-service
      summary: Continue the original quota definition operation with its server-stored actor, Key and request; never starts a new operation
      security: [{ UserBearerAuth: [] }]
      parameters:
        - name: operationId
          in: path
          required: true
          schema: { $ref: './common.yaml#/components/schemas/UuidV7' }
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              maxProperties: 0
      responses:
        '200':
          description: Authoritative result; requires current Platform Admin authorization.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/QuotaDefinitionOperationRecovery' }
        '400': { $ref: './common.yaml#/components/responses/ValidationProblem' }
        '401': { $ref: './common.yaml#/components/responses/Problem' }
        '403': { $ref: './common.yaml#/components/responses/Problem' }
        '404': { $ref: './common.yaml#/components/responses/Problem' }
        '405': { $ref: './common.yaml#/components/responses/Problem' }
        '409': { $ref: './common.yaml#/components/responses/Problem' }
        '502': { $ref: './common.yaml#/components/responses/Problem' }
        '503': { $ref: './common.yaml#/components/responses/Problem' }
        '504': { $ref: './common.yaml#/components/responses/Problem' }

  /api/v1/platform/quota-definitions:
    get:
      tags: [Platform entitlement bootstrap]
      operationId: listQuotaDefinitions
      x-saasforge-service: entitlement-service
      summary: List Quota Definitions by literal code substring and status
      security: [{ UserBearerAuth: [] }]
      parameters:
        - name: cursor
          in: query
          schema: { type: string, maxLength: 2048 }
        - name: limit
          in: query
          schema: { type: integer, minimum: 1, maximum: 100, default: 50 }
        - name: code
          in: query
          schema: { type: string, maxLength: 100 }
        - name: status
          in: query
          schema: { $ref: '#/components/schemas/QuotaDefinitionStatus' }
      responses:
        '200':
          description: Authoritative result; requires current Platform Admin authorization.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/QuotaDefinitionPage' }
        '400': { $ref: './common.yaml#/components/responses/ValidationProblem' }
        '401': { $ref: './common.yaml#/components/responses/Problem' }
        '403': { $ref: './common.yaml#/components/responses/Problem' }
        '404': { $ref: './common.yaml#/components/responses/Problem' }
        '405': { $ref: './common.yaml#/components/responses/Problem' }
        '409': { $ref: './common.yaml#/components/responses/Problem' }
        '502': { $ref: './common.yaml#/components/responses/Problem' }
        '503': { $ref: './common.yaml#/components/responses/Problem' }
        '504': { $ref: './common.yaml#/components/responses/Problem' }
    post:
      tags: [Platform entitlement bootstrap]
      operationId: createQuotaDefinition
      x-saasforge-service: entitlement-service
      x-saasforge-java-sdk: true
      summary: Create the max_users Quota Definition in DRAFT state
      description: Once registered, the actor and Key identify the original operation. After its 24-hour replay window, this entrypoint also rejects that Key with QUOTA_DEFINITION_RECOVERY_EXPIRED; read the actor-private operation record to verify the result instead of starting again.
      security: [{ UserBearerAuth: [] }]
      parameters:
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/CreateQuotaDefinitionRequest' }
      responses:
        '201':
          description: The Quota Definition was created in DRAFT state.
          headers:
            Location: { $ref: './common.yaml#/components/headers/Location' }
          content:
            application/json:
              schema: { $ref: '#/components/schemas/QuotaDefinition' }
        '400': { $ref: './common.yaml#/components/responses/ValidationProblem' }
        '401': { $ref: './common.yaml#/components/responses/Problem' }
        '403': { $ref: './common.yaml#/components/responses/Problem' }
        '409': { $ref: './common.yaml#/components/responses/Problem' }
        '404': { $ref: './common.yaml#/components/responses/Problem' }
        '405': { $ref: './common.yaml#/components/responses/Problem' }
        '502': { $ref: './common.yaml#/components/responses/Problem' }
        '504': { $ref: './common.yaml#/components/responses/Problem' }

  /api/v1/platform/quota-definitions/{quotaDefinitionId}:
    get:
      tags: [Platform entitlement bootstrap]
      operationId: getQuotaDefinition
      x-saasforge-service: entitlement-service
      summary: Read authoritative Quota Definition detail
      security: [{ UserBearerAuth: [] }]
      parameters:
        - name: quotaDefinitionId
          in: path
          required: true
          schema: { $ref: './common.yaml#/components/schemas/UuidV7' }
      responses:
        '200':
          description: Authoritative result; requires current Platform Admin authorization.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/QuotaDefinition' }
        '400': { $ref: './common.yaml#/components/responses/ValidationProblem' }
        '401': { $ref: './common.yaml#/components/responses/Problem' }
        '403': { $ref: './common.yaml#/components/responses/Problem' }
        '404': { $ref: './common.yaml#/components/responses/Problem' }
        '405': { $ref: './common.yaml#/components/responses/Problem' }
        '409': { $ref: './common.yaml#/components/responses/Problem' }
        '502': { $ref: './common.yaml#/components/responses/Problem' }
        '503': { $ref: './common.yaml#/components/responses/Problem' }
        '504': { $ref: './common.yaml#/components/responses/Problem' }

  /api/v1/platform/quota-definitions/{quotaDefinitionId}/activations:
    post:
      tags: [Platform entitlement bootstrap]
      operationId: activateQuotaDefinition
      x-saasforge-service: entitlement-service
      x-saasforge-java-sdk: true
      summary: Activate a DRAFT Quota Definition
      description: Once registered, the actor and Key identify the original operation. After its 24-hour replay window, this entrypoint also rejects that Key with QUOTA_DEFINITION_RECOVERY_EXPIRED; read the actor-private operation record to verify the result instead of starting again.
      security: [{ UserBearerAuth: [] }]
      parameters:
        - $ref: '#/components/parameters/QuotaDefinitionId'
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        description: Browser clients send an empty JSON object to satisfy the browser request media-type boundary.
        required: false
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              maxProperties: 0
      responses:
        '200':
          description: The Quota Definition is ACTIVE.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/QuotaDefinition' }
        '400': { $ref: './common.yaml#/components/responses/ValidationProblem' }
        '401': { $ref: './common.yaml#/components/responses/Problem' }
        '403': { $ref: './common.yaml#/components/responses/Problem' }
        '404': { $ref: './common.yaml#/components/responses/Problem' }
        '409': { $ref: './common.yaml#/components/responses/Problem' }
        '405': { $ref: './common.yaml#/components/responses/Problem' }
        '502': { $ref: './common.yaml#/components/responses/Problem' }
        '504': { $ref: './common.yaml#/components/responses/Problem' }

  /api/v1/platform/tenants/{tenantId}/subscription-operations:
    get:
      tags: [Platform entitlement bootstrap]
      operationId: listSubscriptionOperations
      x-saasforge-service: entitlement-service
      summary: Find only the current actor Subscription operation attempts after losing an in-memory handle
      security: [{ UserBearerAuth: [] }]
      parameters:
        - $ref: '#/components/parameters/TenantId'
        - name: cursor
          in: query
          schema: { type: string, maxLength: 2048 }
        - name: limit
          in: query
          schema: { type: integer, minimum: 1, maximum: 100, default: 50 }
      responses:
        '200':
          description: Authoritative result; requires current Platform Admin authorization.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/SubscriptionOperationPage' }
        '400': { $ref: './common.yaml#/components/responses/ValidationProblem' }
        '401': { $ref: './common.yaml#/components/responses/Problem' }
        '403': { $ref: './common.yaml#/components/responses/Problem' }
        '404': { $ref: './common.yaml#/components/responses/Problem' }
        '405': { $ref: './common.yaml#/components/responses/Problem' }
        '409': { $ref: './common.yaml#/components/responses/Problem' }
        '502': { $ref: './common.yaml#/components/responses/Problem' }
        '503': { $ref: './common.yaml#/components/responses/Problem' }
        '504': { $ref: './common.yaml#/components/responses/Problem' }

  /api/v1/platform/subscription-operations/{operationId}:
    get:
      tags: [Platform entitlement bootstrap]
      operationId: getSubscriptionOperation
      x-saasforge-service: entitlement-service
      summary: Read own Subscription operation result; missing or expired records never authorize a new Subscription operation
      security: [{ UserBearerAuth: [] }]
      parameters:
        - name: operationId
          in: path
          required: true
          schema: { $ref: './common.yaml#/components/schemas/UuidV7' }
      responses:
        '200':
          description: Authoritative result; requires current Platform Admin authorization.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/SubscriptionOperationRecovery' }
        '400': { $ref: './common.yaml#/components/responses/ValidationProblem' }
        '401': { $ref: './common.yaml#/components/responses/Problem' }
        '403': { $ref: './common.yaml#/components/responses/Problem' }
        '404': { $ref: './common.yaml#/components/responses/Problem' }
        '405': { $ref: './common.yaml#/components/responses/Problem' }
        '409': { $ref: './common.yaml#/components/responses/Problem' }
        '502': { $ref: './common.yaml#/components/responses/Problem' }
        '503': { $ref: './common.yaml#/components/responses/Problem' }
        '504': { $ref: './common.yaml#/components/responses/Problem' }

  /api/v1/platform/subscription-operations/{operationId}/recovery:
    post:
      tags: [Platform entitlement bootstrap]
      operationId: recoverSubscriptionOperation
      x-saasforge-service: entitlement-service
      summary: Continue the original Subscription operation with its server-stored actor, Key and request; never starts a new operation
      security: [{ UserBearerAuth: [] }]
      parameters:
        - name: operationId
          in: path
          required: true
          schema: { $ref: './common.yaml#/components/schemas/UuidV7' }
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              maxProperties: 0
      responses:
        '200':
          description: Authoritative result; requires current Platform Admin authorization.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/SubscriptionOperationRecovery' }
        '400': { $ref: './common.yaml#/components/responses/ValidationProblem' }
        '401': { $ref: './common.yaml#/components/responses/Problem' }
        '403': { $ref: './common.yaml#/components/responses/Problem' }
        '404': { $ref: './common.yaml#/components/responses/Problem' }
        '405': { $ref: './common.yaml#/components/responses/Problem' }
        '409': { $ref: './common.yaml#/components/responses/Problem' }
        '502': { $ref: './common.yaml#/components/responses/Problem' }
        '503': { $ref: './common.yaml#/components/responses/Problem' }
        '504': { $ref: './common.yaml#/components/responses/Problem' }

  /api/v1/platform/plan-operations:
    get:
      tags: [Platform entitlement bootstrap]
      operationId: listPlanOperations
      x-saasforge-service: entitlement-service
      summary: Find only the current actor Plan operation attempts after losing an in-memory handle
      security: [{ UserBearerAuth: [] }]
      parameters:
        - name: cursor
          in: query
          schema: { type: string, maxLength: 2048 }
        - name: limit
          in: query
          schema: { type: integer, minimum: 1, maximum: 100, default: 50 }
      responses:
        '200':
          description: Authoritative result; requires current Platform Admin authorization.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/PlanOperationPage' }
        '400': { $ref: './common.yaml#/components/responses/ValidationProblem' }
        '401': { $ref: './common.yaml#/components/responses/Problem' }
        '403': { $ref: './common.yaml#/components/responses/Problem' }
        '404': { $ref: './common.yaml#/components/responses/Problem' }
        '405': { $ref: './common.yaml#/components/responses/Problem' }
        '409': { $ref: './common.yaml#/components/responses/Problem' }
        '502': { $ref: './common.yaml#/components/responses/Problem' }
        '503': { $ref: './common.yaml#/components/responses/Problem' }
        '504': { $ref: './common.yaml#/components/responses/Problem' }

  /api/v1/platform/plan-operations/{operationId}:
    get:
      tags: [Platform entitlement bootstrap]
      operationId: getPlanOperation
      x-saasforge-service: entitlement-service
      summary: Read own Plan operation result; missing or expired records never authorize a new Plan operation
      security: [{ UserBearerAuth: [] }]
      parameters:
        - name: operationId
          in: path
          required: true
          schema: { $ref: './common.yaml#/components/schemas/UuidV7' }
      responses:
        '200':
          description: Authoritative result; requires current Platform Admin authorization.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/PlanOperationRecovery' }
        '400': { $ref: './common.yaml#/components/responses/ValidationProblem' }
        '401': { $ref: './common.yaml#/components/responses/Problem' }
        '403': { $ref: './common.yaml#/components/responses/Problem' }
        '404': { $ref: './common.yaml#/components/responses/Problem' }
        '405': { $ref: './common.yaml#/components/responses/Problem' }
        '409': { $ref: './common.yaml#/components/responses/Problem' }
        '502': { $ref: './common.yaml#/components/responses/Problem' }
        '503': { $ref: './common.yaml#/components/responses/Problem' }
        '504': { $ref: './common.yaml#/components/responses/Problem' }

  /api/v1/platform/plan-operations/{operationId}/recovery:
    post:
      tags: [Platform entitlement bootstrap]
      operationId: recoverPlanOperation
      x-saasforge-service: entitlement-service
      summary: Continue the original Plan operation with its server-stored actor, Key and request; never starts a new operation
      security: [{ UserBearerAuth: [] }]
      parameters:
        - name: operationId
          in: path
          required: true
          schema: { $ref: './common.yaml#/components/schemas/UuidV7' }
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              maxProperties: 0
      responses:
        '200':
          description: Authoritative result; requires current Platform Admin authorization.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/PlanOperationRecovery' }
        '400': { $ref: './common.yaml#/components/responses/ValidationProblem' }
        '401': { $ref: './common.yaml#/components/responses/Problem' }
        '403': { $ref: './common.yaml#/components/responses/Problem' }
        '404': { $ref: './common.yaml#/components/responses/Problem' }
        '405': { $ref: './common.yaml#/components/responses/Problem' }
        '409': { $ref: './common.yaml#/components/responses/Problem' }
        '502': { $ref: './common.yaml#/components/responses/Problem' }
        '503': { $ref: './common.yaml#/components/responses/Problem' }
        '504': { $ref: './common.yaml#/components/responses/Problem' }

  /api/v1/platform/plans/{planId}:
    get:
      tags: [Platform entitlement bootstrap]
      operationId: getPlan
      x-saasforge-service: entitlement-service
      summary: Read authoritative Plan detail
      security: [{ UserBearerAuth: [] }]
      parameters:
        - name: planId
          in: path
          required: true
          schema: { $ref: './common.yaml#/components/schemas/UuidV7' }
      responses:
        '200':
          description: Authoritative result; requires current Platform Admin authorization.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Plan' }
        '400': { $ref: './common.yaml#/components/responses/ValidationProblem' }
        '401': { $ref: './common.yaml#/components/responses/Problem' }
        '403': { $ref: './common.yaml#/components/responses/Problem' }
        '404': { $ref: './common.yaml#/components/responses/Problem' }
        '405': { $ref: './common.yaml#/components/responses/Problem' }
        '409': { $ref: './common.yaml#/components/responses/Problem' }
        '502': { $ref: './common.yaml#/components/responses/Problem' }
        '503': { $ref: './common.yaml#/components/responses/Problem' }
        '504': { $ref: './common.yaml#/components/responses/Problem' }

  /api/v1/platform/plans:
    get:
      tags: [Platform entitlement bootstrap]
      operationId: listPlans
      x-saasforge-service: entitlement-service
      summary: List Plans by literal code substring and status
      security: [{ UserBearerAuth: [] }]
      parameters:
        - name: cursor
          in: query
          schema: { type: string, maxLength: 2048 }
        - name: limit
          in: query
          schema: { type: integer, minimum: 1, maximum: 100, default: 50 }
        - name: code
          in: query
          schema: { type: string, maxLength: 100 }
        - name: status
          in: query
          schema: { $ref: '#/components/schemas/PlanStatus' }
      responses:
        '200':
          description: Authoritative result; requires current Platform Admin authorization.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/PlanPage' }
        '400': { $ref: './common.yaml#/components/responses/ValidationProblem' }
        '401': { $ref: './common.yaml#/components/responses/Problem' }
        '403': { $ref: './common.yaml#/components/responses/Problem' }
        '404': { $ref: './common.yaml#/components/responses/Problem' }
        '405': { $ref: './common.yaml#/components/responses/Problem' }
        '409': { $ref: './common.yaml#/components/responses/Problem' }
        '502': { $ref: './common.yaml#/components/responses/Problem' }
        '503': { $ref: './common.yaml#/components/responses/Problem' }
        '504': { $ref: './common.yaml#/components/responses/Problem' }
    post:
      tags: [Platform entitlement bootstrap]
      operationId: createPlan
      x-saasforge-service: entitlement-service
      x-saasforge-java-sdk: true
      summary: Create a DRAFT Plan with its max_users limit
      description: New requests require max_users at least 1. Original actor/Key and exact fields may replay a retained historical zero result without granting again. Registered operations cannot restart after their 24-hour replay deadline; stable results remain readable through plan-operations.
      security: [{ UserBearerAuth: [] }]
      parameters:
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/CreatePlanRequest' }
      responses:
        '201':
          description: The Plan was created in DRAFT state.
          headers:
            Location: { $ref: './common.yaml#/components/headers/Location' }
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Plan' }
        '400': { $ref: './common.yaml#/components/responses/ValidationProblem' }
        '401': { $ref: './common.yaml#/components/responses/Problem' }
        '403': { $ref: './common.yaml#/components/responses/Problem' }
        '409': { $ref: './common.yaml#/components/responses/Problem' }
        '404': { $ref: './common.yaml#/components/responses/Problem' }
        '405': { $ref: './common.yaml#/components/responses/Problem' }
        '502': { $ref: './common.yaml#/components/responses/Problem' }
        '504': { $ref: './common.yaml#/components/responses/Problem' }

  /api/v1/platform/plans/{planId}/activations:
    post:
      tags: [Platform entitlement bootstrap]
      operationId: activatePlan
      x-saasforge-service: entitlement-service
      x-saasforge-java-sdk: true
      summary: Activate a DRAFT Plan
      description: New activations require max_users at least 1. Retained historical operation results replay unchanged; no automatic increase is performed.
      security: [{ UserBearerAuth: [] }]
      parameters:
        - $ref: '#/components/parameters/PlanId'
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        description: Browser clients send an empty JSON object to satisfy the browser request media-type boundary.
        required: false
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              maxProperties: 0
      responses:
        '200':
          description: The Plan is ACTIVE.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Plan' }
        '400': { $ref: './common.yaml#/components/responses/ValidationProblem' }
        '401': { $ref: './common.yaml#/components/responses/Problem' }
        '403': { $ref: './common.yaml#/components/responses/Problem' }
        '404': { $ref: './common.yaml#/components/responses/Problem' }
        '409': { $ref: './common.yaml#/components/responses/Problem' }
        '405': { $ref: './common.yaml#/components/responses/Problem' }
        '502': { $ref: './common.yaml#/components/responses/Problem' }
        '504': { $ref: './common.yaml#/components/responses/Problem' }

  /api/v1/platform/tenants/{tenantId}/subscription:
    get:
      tags: [Platform entitlement bootstrap]
      operationId: getTenantSubscription
      x-saasforge-service: entitlement-service
      summary: Read the Tenant subscription and authoritative max_users usage
      description: Entitlement-owned read. Null subscription means no grant; failures never mean absent subscription or zero usage. Effectiveness is evaluated at observedAt separately from persisted ACTIVE. No Tenant lifecycle facts are inferred.
      security: [{ UserBearerAuth: [] }]
      parameters:
        - $ref: '#/components/parameters/TenantId'
      responses:
        '200':
          description: Authoritative subscription and quota observation.
          headers:
            Cache-Control: { schema: { type: string, const: no-store } }
          content:
            application/json:
              schema: { $ref: '#/components/schemas/TenantSubscription' }
        '400': { $ref: './common.yaml#/components/responses/ValidationProblem' }
        '401': { $ref: './common.yaml#/components/responses/Problem' }
        '403': { $ref: './common.yaml#/components/responses/Problem' }
        '405': { $ref: './common.yaml#/components/responses/Problem' }
        '502': { $ref: './common.yaml#/components/responses/Problem' }
        '503': { $ref: './common.yaml#/components/responses/Problem' }
        '504': { $ref: './common.yaml#/components/responses/Problem' }

  /api/v1/platform/tenants/{tenantId}/subscriptions:
    post:
      tags: [Platform entitlement bootstrap]
      operationId: createInitialSubscription
      x-saasforge-service: entitlement-service
      x-saasforge-java-sdk: true
      summary: Create the first active Subscription for a Tenant
      description: Requires a Platform Admin and an ACTIVE Plan with max_users at least 1 for new grants. Retained original-operation replays and existing zero-quota entitlements are unchanged. This batch supports only immediate ACTIVE Subscriptions.
      security: [{ UserBearerAuth: [] }]
      parameters:
        - $ref: '#/components/parameters/TenantId'
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/CreateInitialSubscriptionRequest' }
      responses:
        '201':
          description: The first active Subscription was created.
          headers:
            Location: { $ref: './common.yaml#/components/headers/Location' }
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Subscription' }
        '400': { $ref: './common.yaml#/components/responses/ValidationProblem' }
        '401': { $ref: './common.yaml#/components/responses/Problem' }
        '403': { $ref: './common.yaml#/components/responses/Problem' }
        '404': { $ref: './common.yaml#/components/responses/Problem' }
        '409': { $ref: './common.yaml#/components/responses/Problem' }
        '405': { $ref: './common.yaml#/components/responses/Problem' }
        '502': { $ref: './common.yaml#/components/responses/Problem' }
        '504': { $ref: './common.yaml#/components/responses/Problem' }

  /api/v1/platform/oauth-clients:
    post:
      tags: [OAuth clients]
      operationId: createOAuthClient
      x-saasforge-service: iam-service
      x-saasforge-java-sdk: true
      summary: Create an OAuth Client and reveal its Secret once
      security: [{ UserBearerAuth: [] }]
      parameters:
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/CreateOAuthClientRequest' }
      responses:
        '201':
          description: The OAuth Client was created. Store clientSecret now; it cannot be retrieved again.
          headers:
            Location: { $ref: './common.yaml#/components/headers/Location' }
          content:
            application/json:
              schema: { $ref: '#/components/schemas/OAuthClientSecretResult' }
        '400': { $ref: './common.yaml#/components/responses/ValidationProblem' }
        '401': { $ref: './common.yaml#/components/responses/Problem' }
        '403': { $ref: './common.yaml#/components/responses/Problem' }
        '409': { $ref: './common.yaml#/components/responses/Problem' }
        '404': { $ref: './common.yaml#/components/responses/Problem' }
        '405': { $ref: './common.yaml#/components/responses/Problem' }
        '502': { $ref: './common.yaml#/components/responses/Problem' }
        '504': { $ref: './common.yaml#/components/responses/Problem' }

  /api/v1/platform/oauth-clients/{clientId}:
    get:
      tags: [OAuth clients]
      operationId: getOAuthClient
      x-saasforge-service: iam-service
      x-saasforge-java-sdk: true
      summary: Read non-sensitive OAuth Client details
      security: [{ UserBearerAuth: [] }]
      parameters:
        - $ref: '#/components/parameters/ClientId'
      responses:
        '200':
          description: Non-sensitive immutable configuration and lifecycle state.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/OAuthClientDetail' }
        '401': { $ref: './common.yaml#/components/responses/Problem' }
        '403': { $ref: './common.yaml#/components/responses/Problem' }
        '404': { $ref: './common.yaml#/components/responses/Problem' }
        '405': { $ref: './common.yaml#/components/responses/Problem' }
        '502': { $ref: './common.yaml#/components/responses/Problem' }
        '504': { $ref: './common.yaml#/components/responses/Problem' }

  /api/v1/platform/oauth-clients/{clientId}/secret-rotations:
    post:
      tags: [OAuth clients]
      operationId: rotateOAuthClientSecret
      x-saasforge-service: iam-service
      x-saasforge-java-sdk: true
      summary: Rotate an OAuth Client Secret and reveal the new Secret once
      description: The previous and new Secret overlap for the service-controlled rotation window; revocation ends all Client access immediately.
      security: [{ UserBearerAuth: [] }]
      parameters:
        - $ref: '#/components/parameters/ClientId'
        - $ref: '#/components/parameters/IdempotencyKey'
      responses:
        '200':
          description: The Secret was rotated. Store clientSecret now; it cannot be retrieved again.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/OAuthClientSecretResult' }
        '400': { $ref: './common.yaml#/components/responses/ValidationProblem' }
        '401': { $ref: './common.yaml#/components/responses/Problem' }
        '403': { $ref: './common.yaml#/components/responses/Problem' }
        '404': { $ref: './common.yaml#/components/responses/Problem' }
        '409': { $ref: './common.yaml#/components/responses/Problem' }
        '405': { $ref: './common.yaml#/components/responses/Problem' }
        '502': { $ref: './common.yaml#/components/responses/Problem' }
        '504': { $ref: './common.yaml#/components/responses/Problem' }

  /api/v1/platform/oauth-clients/{clientId}/secret-issuance-recoveries:
    post:
      tags: [OAuth clients]
      operationId: recoverOAuthClientSecret
      x-saasforge-service: iam-service
      x-saasforge-java-sdk: true
      summary: Replace one lost OAuth Client Secret issuance result
      description: The original successful create or rotation can be recovered once by its original actor within ten minutes. No prior plaintext is retained or replayed.
      security: [{ UserBearerAuth: [] }]
      parameters:
        - $ref: '#/components/parameters/ClientId'
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/SecretIssuanceRecoveryRequest' }
      responses:
        '200':
          description: The possibly undelivered Secret was invalidated and its replacement is revealed once.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/OAuthClientSecretResult' }
        '400': { $ref: './common.yaml#/components/responses/ValidationProblem' }
        '401': { $ref: './common.yaml#/components/responses/Problem' }
        '403': { $ref: './common.yaml#/components/responses/Problem' }
        '404': { $ref: './common.yaml#/components/responses/Problem' }
        '409': { $ref: './common.yaml#/components/responses/Problem' }
        '405': { $ref: './common.yaml#/components/responses/Problem' }
        '502': { $ref: './common.yaml#/components/responses/Problem' }
        '504': { $ref: './common.yaml#/components/responses/Problem' }

  /api/v1/platform/oauth-clients/{clientId}/revocations:
    post:
      tags: [OAuth clients]
      operationId: revokeOAuthClient
      x-saasforge-service: iam-service
      x-saasforge-java-sdk: true
      summary: Irreversibly revoke an OAuth Client
      security: [{ UserBearerAuth: [] }]
      parameters:
        - $ref: '#/components/parameters/ClientId'
        - $ref: '#/components/parameters/IdempotencyKey'
      responses:
        '204': { $ref: './common.yaml#/components/responses/NoContent' }
        '400': { $ref: './common.yaml#/components/responses/ValidationProblem' }
        '401': { $ref: './common.yaml#/components/responses/Problem' }
        '403': { $ref: './common.yaml#/components/responses/Problem' }
        '404': { $ref: './common.yaml#/components/responses/Problem' }
        '409': { $ref: './common.yaml#/components/responses/Problem' }
        '405': { $ref: './common.yaml#/components/responses/Problem' }
        '502': { $ref: './common.yaml#/components/responses/Problem' }
        '504': { $ref: './common.yaml#/components/responses/Problem' }

components:
  securitySchemes:
    UserBearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
      description: A Platform Admin user Access Token. OAuth Client tokens cannot access platform management operations.
    OAuthClientBasic:
      type: http
      scheme: basic
      description: OAuth Client client_id and client_secret.
    PlatformRefreshCookieAuth:
      type: apiKey
      in: cookie
      name: __Host-sf_platform_refresh
      description: Platform Browser Session Slot Refresh Token cookie used only by IAM session operations.
    TenantRefreshCookieAuth:
      type: apiKey
      in: cookie
      name: __Host-sf_tenant_refresh
      description: Tenant Browser Session Slot Refresh Token cookie used only by IAM session operations.
    ServiceOAuth2:
      type: oauth2
      description: Service Access Token issued through OAuth 2.0 Client Credentials.
      flows:
        clientCredentials:
          tokenUrl: /oauth2/token
          scopes:
            runtime:read: Read runtime entitlement state.
            runtime:quota:write: Apply runtime quota operations.
  headers:
    RefreshCookie:
      description: A rotated slot-specific HttpOnly, Secure, SameSite=Strict host-only cookie scoped to Path=/.
      required: true
      schema: { type: string }
    ClearedRefreshCookie:
      description: One or more expired slot-specific or legacy Refresh Token cookies with Path=/.
      required: true
      schema: { type: string }
    JwksCacheControl:
      description: JWKS cache policy.
      required: true
      schema:
        type: string
        const: max-age=300
  parameters:
    IdempotencyKey:
      name: Idempotency-Key
      in: header
      required: true
      schema: { $ref: './common.yaml#/components/schemas/UuidV7' }
    CsrfHeader:
      name: X-SF-CSRF
      in: header
      required: true
      schema:
        type: string
        const: '1'
    BrowserOrigin:
      name: Origin
      in: header
      required: true
      description: Must exactly match the configured platform or tenant console HTTPS Origin.
      schema:
        type: string
        format: uri
    FetchSite:
      name: Sec-Fetch-Site
      in: header
      required: false
      description: When supplied by the browser, cross-site is rejected; absence still requires an exact Origin match.
      schema:
        type: string
        enum: [same-origin, same-site, none]
    PlatformRefreshTokenCookie:
      name: __Host-sf_platform_refresh
      in: cookie
      required: true
      schema:
        type: string
        minLength: 1
    TenantRefreshTokenCookie:
      name: __Host-sf_tenant_refresh
      in: cookie
      required: true
      schema:
        type: string
        minLength: 1
    OptionalPlatformRefreshTokenCookie:
      name: __Host-sf_platform_refresh
      in: cookie
      required: false
      schema:
        type: string
        minLength: 1
    OptionalTenantRefreshTokenCookie:
      name: __Host-sf_tenant_refresh
      in: cookie
      required: false
      schema:
        type: string
        minLength: 1
    TenantId:
      name: tenantId
      in: path
      required: true
      schema: { $ref: './common.yaml#/components/schemas/UuidV7' }
    QuotaDefinitionId:
      name: quotaDefinitionId
      in: path
      required: true
      schema: { $ref: './common.yaml#/components/schemas/UuidV7' }
    PlanId:
      name: planId
      in: path
      required: true
      schema: { $ref: './common.yaml#/components/schemas/UuidV7' }
    ClientId:
      name: clientId
      in: path
      required: true
      schema: { $ref: './common.yaml#/components/schemas/UuidV7' }
  schemas:
    CurrentSession:
      type: object
      additionalProperties: false
      required: [identityId, email, platformAdmin]
      properties:
        identityId: { $ref: './common.yaml#/components/schemas/UuidV7' }
        email: { type: string, format: email }
        displayName: { type: string, minLength: 1, maxLength: 200 }
        platformAdmin:
          type: boolean
          description: Current active PLATFORM_ADMIN assignment; not a grant for any later operation.
    Timestamp:
      type: string
      format: date-time
      pattern: '^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}[.][0-9]{3}Z$'
    Email:
      type: string
      format: email
      minLength: 3
      maxLength: 320
    EmptyRequest:
      type: object
      additionalProperties: false
    SessionSlotRequest:
      type: object
      required: [sessionSlot]
      properties:
        sessionSlot:
          type: string
          enum: [PLATFORM, TENANT]
      additionalProperties: false
    LoginRequest:
      type: object
      required: [email, password]
      properties:
        email: { $ref: '#/components/schemas/Email' }
        password:
          type: string
          format: password
          minLength: 1
          writeOnly: true
        contextType:
          type: string
          enum: [PLATFORM, TENANT]
          default: TENANT
      additionalProperties: false
    PasswordChangeRequest:
      type: object
      required: [newPassword]
      properties:
        newPassword:
          type: string
          format: password
          minLength: 1
          writeOnly: true
      additionalProperties: false
    PasswordSetupRequest:
      type: object
      required: [token, newPassword]
      properties:
        token:
          type: string
          minLength: 43
          maxLength: 43
          pattern: '^[A-Za-z0-9_-]{43}$'
          writeOnly: true
        newPassword:
          type: string
          format: password
          minLength: 1
          writeOnly: true
      additionalProperties: false
    AccessTokenResult:
      type: object
      required: [contextState, accessToken, tokenType, expiresIn]
      properties:
        contextState: { const: ACCESS_TOKEN_ISSUED }
        accessToken: { type: string, minLength: 1 }
        tokenType: { type: string, const: Bearer }
        expiresIn: { type: integer, format: int64, minimum: 1 }
        tenantContext:
          $ref: '#/components/schemas/TenantAuthenticationContext'
      additionalProperties: false
    MembershipCandidate:
      type: object
      required: [membershipId, tenantId, tenantDisplayName]
      properties:
        membershipId: { $ref: './common.yaml#/components/schemas/UuidV7' }
        tenantId: { $ref: './common.yaml#/components/schemas/UuidV7' }
        tenantDisplayName: { type: string, minLength: 1, maxLength: 200 }
      additionalProperties: false
    TenantAuthenticationContext:
      type: object
      required:
        [membershipId, tenantId, tenantDisplayName, accessibleMemberships]
      properties:
        membershipId: { $ref: './common.yaml#/components/schemas/UuidV7' }
        tenantId: { $ref: './common.yaml#/components/schemas/UuidV7' }
        tenantDisplayName: { type: string, minLength: 1, maxLength: 200 }
        accessibleMemberships:
          type: array
          minItems: 1
          maxItems: 100
          items: { $ref: '#/components/schemas/MembershipCandidate' }
        brandProfile:
          $ref: '#/components/schemas/TenantBrandProfile'
      additionalProperties: false
    TenantBrandProfile:
      type: object
      required: [displayName, primaryColor, accentColor]
      properties:
        displayName: { type: string, minLength: 1, maxLength: 200 }
        logoUrl: { type: string, minLength: 1, maxLength: 2048 }
        faviconUrl: { type: string, minLength: 1, maxLength: 2048 }
        primaryColor: { type: string, pattern: '^#[0-9A-Fa-f]{6}$' }
        accentColor: { type: string, pattern: '^#[0-9A-Fa-f]{6}$' }
      additionalProperties: false
    ContextSelectionRequiredResult:
      type: object
      required: [contextState, memberships]
      properties:
        contextState: { const: CONTEXT_SELECTION_REQUIRED }
        memberships:
          type: array
          minItems: 2
          items: { $ref: '#/components/schemas/MembershipCandidate' }
      additionalProperties: false
    InitialPasswordChangeRequiredResult:
      type: object
      required: [contextState]
      properties:
        contextState: { const: PASSWORD_CHANGE_REQUIRED }
      additionalProperties: false
    ContextSelectionRequest:
      type: object
      required: [membershipId]
      properties:
        membershipId: { $ref: './common.yaml#/components/schemas/UuidV7' }
      additionalProperties: false
    AuthenticationResult:
      oneOf:
        - $ref: '#/components/schemas/AccessTokenResult'
        - $ref: '#/components/schemas/ContextSelectionRequiredResult'
        - $ref: '#/components/schemas/InitialPasswordChangeRequiredResult'
    TenantSwitchRequest:
      type: object
      required: [membershipId]
      properties:
        membershipId: { $ref: './common.yaml#/components/schemas/UuidV7' }
      additionalProperties: false
    ClientCredentialsTokenRequest:
      type: object
      required: [grant_type]
      properties:
        grant_type: { type: string, const: client_credentials }
        scope:
          type: string
          description: Space-delimited requested scopes, each of which must have been granted to the OAuth Client.
      additionalProperties: false
    ClientCredentialsTokenResponse:
      type: object
      required: [access_token, token_type, expires_in, scope]
      properties:
        access_token: { type: string, minLength: 1 }
        token_type: { type: string, const: Bearer }
        expires_in: { type: integer, minimum: 1 }
        scope:
          type: string
          description: Space-delimited scopes granted to this token.
      additionalProperties: false
    Jwk:
      type: object
      required: [kty, use, alg, kid, n, e]
      properties:
        kty: { type: string, const: RSA }
        use: { type: string, const: sig }
        alg: { type: string, const: RS256 }
        kid: { type: string, minLength: 1 }
        n: { type: string, minLength: 1, description: Base64url-encoded RSA modulus. }
        e: { type: string, minLength: 1, description: Base64url-encoded RSA public exponent. }
      additionalProperties: false
    Jwks:
      type: object
      required: [keys]
      properties:
        keys:
          type: array
          minItems: 1
          items: { $ref: '#/components/schemas/Jwk' }
      additionalProperties: false
    PlanPage:
      allOf:
        - $ref: './common.yaml#/components/schemas/Page'
        - type: object
          properties:
            items:
              type: array
              items: { $ref: '#/components/schemas/Plan' }
            nextCursor:
              oneOf:
                - type: string
                - type: 'null'
            hasMore: { type: boolean }
    TenantSubscription:
      type: object
      required: [observedAt, subscription, effective, maxUsersLimit, maxUsersUsed]
      properties:
        observedAt: { $ref: '#/components/schemas/Timestamp' }
        subscription:
          oneOf:
            - $ref: '#/components/schemas/Subscription'
            - type: 'null'
        effective: { type: boolean, description: Subscription validity at observedAt; does not certify Tenant eligibility. }
        maxUsersLimit:
          type: [integer, 'null']
          minimum: 0
          description: Granted limit including historical zero; null only without Subscription.
        maxUsersUsed:
          type: [integer, 'null']
          minimum: 0
          description: Authoritative cumulative usage; an absent usage row is zero under Entitlement rules. Null only without Subscription.
      additionalProperties: false
    SubscriptionOperationPage:
      allOf:
        - $ref: './common.yaml#/components/schemas/Page'
        - type: object
          properties:
            items:
              type: array
              items: { $ref: '#/components/schemas/SubscriptionOperationRecovery' }
            nextCursor:
              oneOf:
                - type: string
                - type: 'null'
            hasMore: { type: boolean }
    SubscriptionOperationRecovery:
      type: object
      description: Actor-private recovery material. Shared Runtime owns idempotencyKey. NOT_COMMITTED is a point-in-time observation, never authorization to create with a new key. UNKNOWN requires manual verification. Committed result remains known after replay retention ends.
      required: [id, tenantId, state, createdAt, replayUntil, canReplay]
      properties:
        id: { $ref: './common.yaml#/components/schemas/UuidV7' }
        tenantId: { $ref: './common.yaml#/components/schemas/UuidV7' }
        state: { type: string, enum: [COMMITTED, PROCESSING, NOT_COMMITTED, UNKNOWN] }
        createdAt: { $ref: '#/components/schemas/Timestamp' }
        replayUntil: { $ref: '#/components/schemas/Timestamp' }
        canReplay: { type: boolean }
        subscriptionId: { $ref: './common.yaml#/components/schemas/UuidV7' }
        idempotencyKey: { $ref: './common.yaml#/components/schemas/UuidV7' }
      additionalProperties: false
    PlanOperationPage:
      allOf:
        - $ref: './common.yaml#/components/schemas/Page'
        - type: object
          properties:
            items:
              type: array
              items: { $ref: '#/components/schemas/PlanOperationRecovery' }
            nextCursor:
              oneOf:
                - type: string
                - type: 'null'
            hasMore: { type: boolean }
    PlanOperationRecovery:
      type: object
      description: Actor-private recovery material. Shared Runtime owns idempotencyKey. NOT_COMMITTED is a point-in-time observation, never authorization to create with a new key. UNKNOWN requires manual verification. Committed result remains known after replay retention ends.
      required: [id, operation, state, createdAt, replayUntil, canReplay]
      properties:
        id: { $ref: './common.yaml#/components/schemas/UuidV7' }
        operation: { type: string, enum: [CREATE, ACTIVATE] }
        state: { type: string, enum: [COMMITTED, PROCESSING, NOT_COMMITTED, UNKNOWN] }
        createdAt: { $ref: '#/components/schemas/Timestamp' }
        replayUntil: { $ref: '#/components/schemas/Timestamp' }
        canReplay: { type: boolean }
        code: { type: string, description: Original Plan code for CREATE; used to guard only that logical creation. }
        planId: { $ref: './common.yaml#/components/schemas/UuidV7' }
        result: { $ref: '#/components/schemas/Plan' }
        idempotencyKey: { $ref: './common.yaml#/components/schemas/UuidV7' }
      additionalProperties: false
    QuotaDefinitionPage:
      allOf:
        - $ref: './common.yaml#/components/schemas/Page'
        - type: object
          properties:
            items:
              type: array
              items: { $ref: '#/components/schemas/QuotaDefinition' }
            nextCursor:
              oneOf:
                - type: string
                - type: 'null'
            hasMore: { type: boolean }
    QuotaDefinitionOperationPage:
      allOf:
        - $ref: './common.yaml#/components/schemas/Page'
        - type: object
          properties:
            items:
              type: array
              items: { $ref: '#/components/schemas/QuotaDefinitionOperationRecovery' }
            nextCursor:
              oneOf:
                - type: string
                - type: 'null'
            hasMore: { type: boolean }
    QuotaDefinitionOperationRecovery:
      type: object
      description: Actor-private recovery material. Shared Runtime owns idempotencyKey. NOT_COMMITTED is a point-in-time observation, never authorization to create with a new key. UNKNOWN requires manual verification. Committed result remains known after replay retention ends.
      required: [id, operation, state, createdAt, replayUntil, canReplay]
      properties:
        id: { $ref: './common.yaml#/components/schemas/UuidV7' }
        operation: { type: string, enum: [CREATE, ACTIVATE] }
        state: { type: string, enum: [COMMITTED, PROCESSING, NOT_COMMITTED, UNKNOWN] }
        createdAt: { $ref: '#/components/schemas/Timestamp' }
        replayUntil: { $ref: '#/components/schemas/Timestamp' }
        canReplay: { type: boolean }
        quotaDefinitionId: { $ref: './common.yaml#/components/schemas/UuidV7' }
        idempotencyKey: { $ref: './common.yaml#/components/schemas/UuidV7' }
      additionalProperties: false
    TenantPage:
      allOf:
        - $ref: './common.yaml#/components/schemas/Page'
        - type: object
          properties:
            items:
              type: array
              items: { $ref: '#/components/schemas/Tenant' }
            nextCursor:
              oneOf:
                - type: string
                - type: 'null'
            hasMore: { type: boolean }
    TenantCreationPage:
      allOf:
        - $ref: './common.yaml#/components/schemas/Page'
        - type: object
          properties:
            items:
              type: array
              items: { $ref: '#/components/schemas/TenantCreationRecovery' }
            nextCursor:
              oneOf:
                - type: string
                - type: 'null'
            hasMore: { type: boolean }
    TenantCreationRecovery:
      type: object
      description: Actor-private recovery material. Shared Runtime owns idempotencyKey. NOT_COMMITTED is a point-in-time observation, never authorization to create with a new key. UNKNOWN requires manual verification. Committed result remains known after replay retention ends.
      required: [id, displayName, state, createdAt, replayUntil, canReplay]
      properties:
        id: { $ref: './common.yaml#/components/schemas/UuidV7' }
        displayName: { type: string, minLength: 1, maxLength: 200 }
        state: { type: string, enum: [COMMITTED, PROCESSING, NOT_COMMITTED, UNKNOWN] }
        createdAt: { $ref: '#/components/schemas/Timestamp' }
        replayUntil: { $ref: '#/components/schemas/Timestamp' }
        canReplay: { type: boolean }
        tenantId: { $ref: './common.yaml#/components/schemas/UuidV7' }
        idempotencyKey: { $ref: './common.yaml#/components/schemas/UuidV7' }
      additionalProperties: false
    TenantStatus:
      type: string
      enum: [PENDING, ACTIVE, SUSPENDED, CLOSED]
    Tenant:
      type: object
      required: [id, displayName, status, expiresAt, createdAt, updatedAt]
      properties:
        id: { $ref: './common.yaml#/components/schemas/UuidV7' }
        displayName: { type: string, minLength: 1, maxLength: 200 }
        status: { $ref: '#/components/schemas/TenantStatus' }
        expiresAt:
          oneOf:
            - $ref: '#/components/schemas/Timestamp'
            - type: 'null'
        createdAt: { $ref: '#/components/schemas/Timestamp' }
        updatedAt: { $ref: '#/components/schemas/Timestamp' }
      additionalProperties: false
    CreateTenantRequest:
      type: object
      required: [displayName]
      properties:
        displayName: { type: string, minLength: 1, maxLength: 200 }
        expiresAt:
          oneOf:
            - type: string
              format: date-time
            - type: 'null'
      additionalProperties: false
    AdministratorInitializationRequest:
      type: object
      required: [administratorEmail]
      properties:
        administratorEmail: { $ref: '#/components/schemas/Email' }
        administratorDisplayName:
          type: string
          minLength: 1
          maxLength: 200
      additionalProperties: false
    QuotaDefinitionStatus:
      type: string
      enum: [DRAFT, ACTIVE, RETIRED]
    QuotaDefinition:
      type: object
      required: [id, code, status, createdAt, updatedAt]
      properties:
        id: { $ref: './common.yaml#/components/schemas/UuidV7' }
        code: { type: string, const: max_users }
        status: { $ref: '#/components/schemas/QuotaDefinitionStatus' }
        createdAt: { $ref: '#/components/schemas/Timestamp' }
        updatedAt: { $ref: '#/components/schemas/Timestamp' }
      additionalProperties: false
    CreateQuotaDefinitionRequest:
      type: object
      required: [code]
      properties:
        code: { type: string, const: max_users }
      additionalProperties: false
    PlanStatus:
      type: string
      enum: [DRAFT, ACTIVE, RETIRED]
    PlanQuotaLimit:
      type: object
      required: [quotaDefinitionId, limit]
      properties:
        quotaDefinitionId: { $ref: './common.yaml#/components/schemas/UuidV7' }
        limit: { type: integer, minimum: 0 }
      additionalProperties: false
    Plan:
      type: object
      required: [id, code, displayName, status, quotaLimits, createdAt, updatedAt]
      properties:
        id: { $ref: './common.yaml#/components/schemas/UuidV7' }
        code:
          type: string
          pattern: '^[a-z][a-z0-9-]{1,62}$'
        displayName: { type: string, minLength: 1, maxLength: 200 }
        status: { $ref: '#/components/schemas/PlanStatus' }
        quotaLimits:
          type: array
          minItems: 1
          maxItems: 1
          items: { $ref: '#/components/schemas/PlanQuotaLimit' }
        createdAt: { $ref: '#/components/schemas/Timestamp' }
        updatedAt: { $ref: '#/components/schemas/Timestamp' }
      additionalProperties: false
    NewPlanQuotaLimit:
      type: object
      description: New grants require max_users of at least 1. Historical Plan responses retain the zero-capable PlanQuotaLimit schema.
      required: [quotaDefinitionId, limit]
      properties:
        quotaDefinitionId: { $ref: './common.yaml#/components/schemas/UuidV7' }
        limit: { type: integer, minimum: 1 }
      additionalProperties: false
    CreatePlanRequest:
      type: object
      required: [code, displayName, quotaLimits]
      properties:
        code:
          type: string
          pattern: '^[a-z][a-z0-9-]{1,62}$'
        displayName: { type: string, minLength: 1, maxLength: 200 }
        quotaLimits:
          type: array
          minItems: 1
          maxItems: 1
          items: { $ref: '#/components/schemas/NewPlanQuotaLimit' }
      additionalProperties: false
    Subscription:
      type: object
      required: [id, tenantId, planId, status, endsAt, createdAt]
      properties:
        id: { $ref: './common.yaml#/components/schemas/UuidV7' }
        tenantId: { $ref: './common.yaml#/components/schemas/UuidV7' }
        planId: { $ref: './common.yaml#/components/schemas/UuidV7' }
        status: { type: string, const: ACTIVE }
        endsAt:
          oneOf:
            - $ref: '#/components/schemas/Timestamp'
            - type: 'null'
        createdAt: { $ref: '#/components/schemas/Timestamp' }
      additionalProperties: false
    CreateInitialSubscriptionRequest:
      type: object
      required: [planId]
      properties:
        planId: { $ref: './common.yaml#/components/schemas/UuidV7' }
        endsAt:
          oneOf:
            - type: string
              format: date-time
            - type: 'null'
      additionalProperties: false
    RuntimeScope:
      type: string
      enum:
        - 'runtime:read'
        - 'runtime:quota:write'
        - 'tenant-access:membership:read'
        - 'tenant-access:tenant:read'
        - 'iam:identity:write'
        - 'iam:password-setup:write'
        - 'iam:platform-role:read'
        - 'iam:sessions:write'
        - 'entitlement:quota:write'
    OAuthClientType:
      type: string
      enum: [RUNTIME_SERVICE, RESERVED_SERVICE]
    ReservedServiceKey:
      type: string
      enum: [IAM, TENANT_ACCESS, ENTITLEMENT]
    OAuthClientStatus:
      type: string
      enum: [ACTIVE, REVOKED]
    OAuthClient:
      type: object
      required: [clientId, displayName, allowedScopes, status, createdAt, updatedAt]
      properties:
        clientId: { $ref: './common.yaml#/components/schemas/UuidV7' }
        displayName: { type: string, minLength: 1, maxLength: 200 }
        allowedScopes:
          type: array
          minItems: 1
          uniqueItems: true
          items: { $ref: '#/components/schemas/RuntimeScope' }
        status: { $ref: '#/components/schemas/OAuthClientStatus' }
        createdAt: { $ref: '#/components/schemas/Timestamp' }
        updatedAt: { $ref: '#/components/schemas/Timestamp' }
      additionalProperties: false
    CreateOAuthClientRequest:
      type: object
      required: [displayName, allowedScopes]
      properties:
        displayName: { type: string, minLength: 1, maxLength: 200 }
        allowedScopes:
          type: array
          minItems: 1
          uniqueItems: true
          items: { $ref: '#/components/schemas/RuntimeScope' }
      additionalProperties: false
    SecretIssuanceRecoveryRequest:
      type: object
      required: [originalIdempotencyKey]
      properties:
        originalIdempotencyKey: { $ref: './common.yaml#/components/schemas/UuidV7' }
      additionalProperties: false
    OAuthClientSecretResult:
      type: object
      required: [clientId, displayName, allowedScopes, status, createdAt, updatedAt, clientSecret]
      properties:
        clientId: { $ref: './common.yaml#/components/schemas/UuidV7' }
        displayName: { type: string, minLength: 1, maxLength: 200 }
        allowedScopes:
          type: array
          minItems: 1
          uniqueItems: true
          items: { $ref: '#/components/schemas/RuntimeScope' }
        status: { $ref: '#/components/schemas/OAuthClientStatus' }
        createdAt: { $ref: '#/components/schemas/Timestamp' }
        updatedAt: { $ref: '#/components/schemas/Timestamp' }
        clientSecret:
          type: string
          minLength: 1
          writeOnly: true
          description: Returned exactly once. Store it in a controlled secret manager.
      additionalProperties: false
    OAuthClientDetail:
      type: object
      required: [clientId, displayName, clientType, allowedScopes, status, createdAt, updatedAt]
      properties:
        clientId: { $ref: './common.yaml#/components/schemas/UuidV7' }
        displayName: { type: string, minLength: 1, maxLength: 200 }
        clientType: { $ref: '#/components/schemas/OAuthClientType' }
        reservedServiceKey:
          oneOf:
            - $ref: '#/components/schemas/ReservedServiceKey'
            - type: 'null'
        allowedScopes:
          type: array
          minItems: 1
          uniqueItems: true
          items: { $ref: '#/components/schemas/RuntimeScope' }
        status: { $ref: '#/components/schemas/OAuthClientStatus' }
        createdAt: { $ref: '#/components/schemas/Timestamp' }
        updatedAt: { $ref: '#/components/schemas/Timestamp' }
        revokedAt:
          oneOf:
            - $ref: '#/components/schemas/Timestamp'
            - type: 'null'
      additionalProperties: false
