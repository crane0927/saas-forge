# 此文件与 compose.yaml 合并，所有数据卷、网络及应用镜像均归属于本次验收项目。
services:
  postgres:
    ports: !reset []
  redis:
    ports: !reset []
  kafka:
    ports: !reset []
    environment:
      KAFKA_HEAP_OPTS: -Xms256m -Xmx512m
      KAFKA_LISTENERS: INTERNAL://:9092,CONTROLLER://:9093
      KAFKA_ADVERTISED_LISTENERS: INTERNAL://kafka:9092
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: CONTROLLER:PLAINTEXT,INTERNAL:PLAINTEXT
  mailpit:
    ports: !override
      - "127.0.0.1::8025"
  otel-collector:
    ports: !reset []
  nacos:
    ports: !reset []
    environment:
      JVM_XMS: 256m
      JVM_XMX: 256m
      JVM_XMN: 128m
  iam-service:
    image: ${SF_ACCEPTANCE_PROJECT:?SF_ACCEPTANCE_PROJECT is required}/iam-service:acceptance
    build: !reset null
    ports: !reset []
    group_add: ["${E2E_HOST_GID:?E2E_HOST_GID is required}"]
    environment:
      BROWSER_ROOT_DOMAIN: ${SF_ACCEPTANCE_ROOT_DOMAIN:-saas.forge.test}
      PASSWORD_SETUP_PAGE_URI: https://console.${SF_ACCEPTANCE_ROOT_DOMAIN:-saas.forge.test}/password-setup
      JAVA_TOOL_OPTIONS: -Xms64m -Xmx256m
  tenant-access-service:
    image: ${SF_ACCEPTANCE_PROJECT:?SF_ACCEPTANCE_PROJECT is required}/tenant-access-service:acceptance
    build: !reset null
    ports: !reset []
    group_add: ["${E2E_HOST_GID:?E2E_HOST_GID is required}"]
    environment:
      JAVA_TOOL_OPTIONS: -Xms64m -Xmx256m
  entitlement-service:
    image: ${SF_ACCEPTANCE_PROJECT:?SF_ACCEPTANCE_PROJECT is required}/entitlement-service:acceptance
    build: !reset null
    ports: !reset []
    group_add: ["${E2E_HOST_GID:?E2E_HOST_GID is required}"]
    environment:
      JAVA_TOOL_OPTIONS: -Xms64m -Xmx256m
  audit-service:
    image: ${SF_ACCEPTANCE_PROJECT:?SF_ACCEPTANCE_PROJECT is required}/audit-service:acceptance
    build: !reset null
    ports: !reset []
    environment:
      JAVA_TOOL_OPTIONS: -Xms32m -Xmx128m
  gateway:
    image: ${SF_ACCEPTANCE_PROJECT:?SF_ACCEPTANCE_PROJECT is required}/gateway:acceptance
    build: !reset null
    ports: !reset []
    environment:
      BROWSER_ROOT_DOMAIN: ${SF_ACCEPTANCE_ROOT_DOMAIN:-saas.forge.test}
      JAVA_TOOL_OPTIONS: -Xms32m -Xmx192m
  iam-platform-admin-bootstrap:
    image: ${SF_ACCEPTANCE_PROJECT:?SF_ACCEPTANCE_PROJECT is required}/iam-service:acceptance
    build: !reset null
    group_add: ["${E2E_HOST_GID:?E2E_HOST_GID is required}"]
  iam-reserved-service-client-bootstrap:
    image: ${SF_ACCEPTANCE_PROJECT:?SF_ACCEPTANCE_PROJECT is required}/iam-service:acceptance
    build: !reset null
    group_add: ["${E2E_HOST_GID:?E2E_HOST_GID is required}"]
  platform-console:
    extends:
      file: ../../consoles/platform-console/compose.yaml
      service: platform-console
    volumes:
      - ../../consoles/integration-test/dist:/app/acceptance:ro
    user: "${E2E_HOST_UID:?E2E_HOST_UID is required}:${E2E_HOST_GID:?E2E_HOST_GID is required}"
  tenant-console:
    extends:
      file: ../../consoles/tenant-console-shell/compose.yaml
      service: tenant-console
    user: "${E2E_HOST_UID:?E2E_HOST_UID is required}:${E2E_HOST_GID:?E2E_HOST_GID is required}"
    volumes:
      - ../../consoles/integration-test/dist/tenant-console:/app/dist:ro
  console-tls:
    extends:
      file: ../compose/console-tls.yaml
      service: console-tls
    depends_on:
      platform-console: { condition: service_started }
      tenant-console: { condition: service_started }
      gateway: { condition: service_started }
