# Design System 规范

**状态：规则与底层组件选型已确认；正式共享包、系统浅色/深色主题、受控 Tenant 品牌颜色解析、两个 Console 的启动状态与路由焦点接入，共享页面状态、浮层、表单、服务端表格、公共响应式布局，以及代表性 Remote 消费与浏览器边界已实现。五项完整 Profile 解析、素材预加载与 React Shell 原子应用已实现并通过[聚合浏览器验收](acceptance/issue-147-brand-runtime.md)；两个 Console 的真实业务页面仍按对应 Issue 交付。**

本规范定义 Platform Console、Tenant Console Shell 与官方业务 Remote 共用的视觉、组件、反馈、键盘和焦点语义。架构边界见 [ADR 0037](adr/0037-browser-surfaces-use-one-shared-design-system.md)，Tenant 品牌所有权见 [ADR 0036](adr/0036-tenant-access-owns-controlled-tenant-brand-profiles.md)，品牌解析与运行时应用见 [ADR 0042](adr/0042-browser-surfaces-atomically-apply-one-resolved-brand.md)，共享布局的 Remote 消费证据见 [共享响应式布局消费与浏览器验证记录](27-shared-responsive-layout-consumption-verification.md)。本规范本身不替代对应实现与浏览器证据。

## 1. 所有权与消费边界

- 唯一公开 UI 包为 `@saas-forge/design-system`；内部可以按 `tokens`、`icons`、`components`、`patterns`、`styles` 和 `testing` 等稳定子路径组织。
- Platform Console、Tenant Console Shell 与 Remote 不得直接使用底层组件库、导入 Design System 内部文件、定义另一套同类公共组件或注入全局样式。
- Design System 独占全局样式、Theme Provider、公共组件外观和公共交互。`app-runtime` 只拥有启动状态，不拥有启动、加载、失败、404 或致命错误的视觉组件。
- 领域页面只拥有领域数据、领域动作和领域组合，可以使用局部作用域样式排列领域内容，但不得改变公共组件的颜色、形状、状态或交互。
- 缺少无领域含义的能力时，必须先评审并扩展 Design System；不提供复制后再治理的临时例外，也不向消费者暴露任意内部样式入口。

## 2. 版本与 Remote 治理

- MVP 的两个 Console 与全部官方 Remote 使用完全相同的 Design System 版本。
- 前端工作区按既有目录边界自动发现当前及后续新增的 Console 与官方 Remote 并执行消费门禁，不维护需要人工同步的消费者名单。
- Shell 独占全局样式和当前 Theme Provider，Remote 不重复注入全局样式。
- 后续 Manifest 声明 `designSystemVersion`；不一致时必须在执行 Remote 代码前拒绝加载。
- 第三方 Remote、多版本兼容范围和独立升级不属于 MVP，出现真实需求后另行决策。

本次 A「分区列表」样板的边界见 [ADR 0047](adr/0047-console-visual-upgrade-preserves-shared-ui-boundaries.md)，验证状态见 [A 方案验证记录](acceptance/console-tenant-ui-a.md)。

## 3. 视觉基线

- 风格为专业、克制、清晰；颜色主要表达重要操作和状态，不使用装饰性强渐变、发光或大面积高饱和颜色。
- 桌面页面适度紧凑；数据表格可以比普通表单更紧凑，但关键标签、操作和错误信息不得缩小。MVP 不提供用户可切换的密度模式。
- 使用操作系统自带的中英文字体栈，不下载产品专属 Web Font。
- 控件和表面使用轻微圆角；阴影只用于弹窗、浮层等确有层级含义的表面，不以大圆角、卡片或阴影装饰普通布局。
- 空间尺寸来自以 `4px` 为基础的语义间距，常用值为 `4px`、`8px`、`12px`、`16px`、`24px` 和 `32px`；消费者不得引入无语义的任意间距。
- 使用同一套简洁线条图标，且只由 Design System 对外提供。重要操作同时显示图标和文字，删除、停用、恢复等含义不能只靠图标表达；正式引入图标依赖时单独核验并锁定版本。
- 动画只用于解释弹窗、反馈、加载和状态切换，不使用持续或装饰性动画；操作系统要求减少动态效果时取消或大幅缩短。

- A 方案采用浅灰画布、白色内容区，标题直接位于画布上；查询、列表、创建记录按上下顺序独立分区。正文基准为 `14px`，页面标题 `20px`，面板圆角 `8px`，常规内容边距 `24px`、区间距 `16px`。深色采用对应语义表面，不改变信息顺序。

## 4. 主题与品牌

- 同时提供浅色与深色主题，默认跟随操作系统设置；MVP 不增加手动主题开关。
- Platform Brand Profile 是显示名称、Logo、favicon、主色和强调色组成的完整平台默认品牌，MVP 由 Design System 内的版本化构建期常量提供，不扩展当前只承载 API Origin 的 Runtime Config。平台第一版显示名称为 `SaaS Forge`，主色为 `#2563EB`，强调色为 `#C026D3`；正式 Logo 与 favicon 使用用户为 Issue #142 确认的 `Connected Core` 方案，不得使用验收夹具代替。
- Brand Token Set 是从某个有效 Brand Profile 派生的受控运行时视觉值，包含浅色与深色主题所需的主色、强调色及可读前景色。Console 与 Remote 不得直接写入颜色值或改写该 Token Set；平台品牌后续只能经集中替换并重新通过浅色、深色、对比度和视觉快照后发布。
- Tenant Brand Profile 只改变显示名称、Logo、favicon、主色和强调色。已发布 v1 契约中 Logo/favicon 保持可选以避免破坏兼容，但第 4 阶段的新写入只能产生五字段完整 Profile；运行时读取到任一字段缺失或无效时拒绝整份 Tenant Profile，不部分应用。
- Design System 是唯一品牌解析边界：它在提交 Tenant 品牌前校验完整结构、显示名称、颜色及受控同站素材引用，并确认 Logo 与 favicon 成功加载、可解码且 MIME 受允许。不接受任意外部 HTTPS URL；具体受控路径前缀、文件类型、大小与失效占位策略由第 4 阶段素材写入契约冻结。
- 解析成功生成唯一不可变 Resolved Brand Profile，其中包含规范化后的完整 Profile、浅色/深色 Brand Token Set 与 `platform | tenant` 来源。Theme Provider、Shell 显示名称与 Logo、favicon 和浏览器标签页标题只能消费该对象；原始 Tenant Brand Profile 不得越过解析器。
- 共享 React Shell 是唯一品牌运行时应用缝。“原子应用”指先完成解析与素材预加载，再只提交一个 Resolved Brand Profile，保证显示名称、Logo、favicon、标签页标题与 Token 始终来自同一 Profile；它不承诺浏览器对 favicon 的实际绘制与 React/CSS 位于同一帧。
- 未取得权威 Tenant Context、Context 读取中、Tenant Context Switch 已提交但新 Context 未恢复，或 Tenant Profile 解析/素材加载失败时，Shell 均完整使用 Platform Brand Profile；品牌失败不阻断已合法建立的 Tenant Context。拒绝结果只暴露稳定且不含原始值的结构、素材引用、颜色或素材加载原因码与可测试回调，当前不新建遥测系统，不记录原始 Profile。
- Platform Console 标签页标题为 `SaaS Forge Platform Console`；Tenant Console 在未建立 Context 或切换中时为 `SaaS Forge Tenant Console`，Tenant 品牌生效后为“`<Tenant 显示名称> · SaaS Forge Tenant Console`”。Tenant Logo 只在已认证 Tenant Shell 的全局品牌位显示；未认证、恢复、错误与切换中间态显示平台 Logo。
- 品牌管理预览可复用同一解析器与品牌组件，但必须隔离在预览容器内，不改变当前 Shell、favicon 或标签页标题。只有保存成功并获得带 revision/ETag 的权威 Profile 或权威回读后，才能更新真实 Shell。
- Remote 只继承 Shell 已提交的 Brand Token Set 与共享布局，不获取原始或 Resolved Brand Profile，不单独渲染品牌素材，不安装第二个 Theme Provider。
- Tenant 品牌不得改变状态颜色、布局、组件、交互或无障碍约束。

## 5. 页面布局与响应式

- 已认证的两个 Console 使用 `ApplicationShell` 共享导航侧栏，展开宽 `216px`、收起宽 `68px`，顶栏高 `56px`。路由名称、顺序、权限和现有全局操作由消费者提供；不增加占位菜单。
- `45rem` 以下导航改为模态抽屉，Tab 保持在抽屉内，Esc 关闭并恢复触发按钮焦点；品牌 Logo 在紧凑顶栏继续可见。语言选择器在已认证顶栏中只有一份，匿名页维持原位置。
- `ContentPanel` 表达内容分区；`StatusTag` 同时显示状态文字，颜色不能成为唯一信号；`DescriptionList` 表达详情键值。三者由 Design System 提供，不包含 Tenant 业务规则。

- 第一版公共布局只提供三类无领域含义能力：充分利用可用宽度的全宽管理页面、按可用宽度自动换列的响应式内容栅格，以及桌面分栏并在窄屏按文档顺序堆叠的主内容与辅助侧栏。侧栏不得因窄屏自动隐藏。
- 第一版不提供任意列跨度的通用栅格，也不把列表与详情、筛选抽屉等包含领域状态和导航行为的模式固化为公共布局；这些模式必须在出现真实页面需求后另行设计。
- 响应式内容栅格只允许消费者选择由 Design System 定义的内容意图，不暴露列跨度、逐断点列数或任意断点参数。实际列数根据组件容器获得的宽度决定，断点属于 Design System 内部实现，消费者不得读取或覆盖。
- 响应式内容栅格只提供“普通内容”和“紧凑统计”两种意图。普通内容的单项建议最小宽度为 `18rem`、最多三列、间距为 `16px`；紧凑统计的单项建议最小宽度为 `12rem`、最多四列、间距为 `12px`；窄屏均自动恢复单列。表单字段不得使用该通用栅格，继续遵循默认单列和受控 `FormRow` 规则。
- 标准分栏第一版只提供左侧主内容与右侧辅助栏。桌面主内容约占三分之二，辅助栏约占三分之一并限制最大宽度；窄屏按文档顺序堆叠为主内容在前、辅助内容在后，不得通过 CSS 调换视觉与键盘顺序，也不得自动隐藏、折叠或改成抽屉。第一版不支持左侧辅助栏和任意比例。
- 标准分栏的栏间距为 `24px`，辅助栏宽度限制在 `18rem`～`24rem`，组件容器不足 `48rem` 时改为上下排列；主栏必须允许安全收缩，不能把页面撑出横向滚动。这些尺寸由 Design System 控制，消费者不得覆盖。
- `ApplicationShell` 独占已认证页面的 `<main>`，嵌入的 `PageLayout` 使用 `as="section"` 避免嵌套主区域；独立页面的 `PageLayout` 默认仍为 `<main>`；分栏主区域使用普通容器，辅助栏使用 `<aside>` 并要求消费者提供可访问名称或关联标题。响应式内容栅格默认使用普通容器，不添加表示交互式单元格键盘模型的 ARIA `grid` 角色；卡片、列表和章节等内容语义由消费者按真实内容提供。
- `PageLayout` 增加可选的 `width="standard" | "wide"`：默认 `standard` 保持现有 `76rem` 最大宽度，`wide` 使用全部可用宽度但保留统一页面边距。表单继续由 `FormLayout` 保持现有 `40rem` 最大宽度，不增加第三种页面宽度。
- Design System 从根入口新增 `ResponsiveGrid` 与 `SplitLayout`。`ResponsiveGrid` 只接收 `intent="content" | "compact-statistics"` 和内容；`SplitLayout` 只接收主内容、辅助内容，以及二选一的辅助栏可访问名称或关联标题。两者均不暴露列数、跨度、断点、比例、顺序、内部类名或任意样式覆盖入口。
- 响应式布局使用浏览器原生 CSS Grid 与 CSS Container Queries 实现，不向消费者暴露 Ant Design 栅格，也不使用 JavaScript 或 `ResizeObserver` 持续测量宽度。
- 数据表格页面可以充分使用可用宽度；普通表单限制在适合阅读和填写的内容宽度，不把字段无条件拉伸到整个大屏幕。
- 表单默认单列；只有两个短小且关系密切的字段可以并排，窄屏必须恢复单列，视觉、键盘和错误阅读顺序保持一致。
- 固定验证 `1440px`、`1280px`、`768px`、`390px` 和 `360px`。产品以桌面管理为主，但窄屏核心流程必须可完成。
- 窄屏保持表格语义和信息顺序，通过受控横向滚动并优先保留名称与关键操作，不自动将每一行改造成另一套卡片交互。

### 登录页布局

- 借鉴 Skyroc 登录页的构图、表单比例与视觉层次，继续使用 SaaS Forge 平台品牌和已确认的视觉基线；不直接复制参考站的品牌、装饰或功能入口。
- 本轮统一两个 Console 共用的普通登录页及其现有提示状态；首次修改密码、选择 Tenant、会话恢复等相邻认证页面只做必要回归，不主动改造其结构。
- 仅调整视觉与布局，保留邮箱、密码、显示密码、登录和语言切换，以及现有密码清理、错误处理、登录中状态和安全回跳。不得增加注册、验证码、找回密码、第三方登录或占位入口。
- 两个登录入口共用布局与平台品牌，以紧贴系统名称右上方的“平台管理”与“租户控制台”角标区分，不增加入口切换按钮。
- 现有成功、冲突和错误提示放在登录标题下、输入框上方，保留处理动作；长提示允许页面滚动，不遮挡表单。
- 密码显示/隐藏使用眼睛图标按钮，保留随状态和语言变化的可访问名称及键盘操作。
- 唯一语言选择器放在页面右上角，窄屏保持可见；切换语言不清空输入、不打断正在进行的登录。
- 采用居中单卡片，内部依次为平台品牌、入口标识、登录标题、提示与表单。表单内容最大宽约 `400px`，卡片内边距 `32px`，窄屏缩小边距；长内容正常滚动。
- 背景延续浅灰画布与白色弱边框卡片，深色使用对应语义表面；不引入波形、翻转文字或模糊动画。登录输入框与全宽按钮高 `40px`、轻微圆角，保留上方可见标签。
- 隔离原型已获确认，正式页面通过共享 `LoginLayout` 实现；认证行为仍由 React Shell 管理。英文平台入口角标使用简短的“Platform admin”。验证范围见[登录页改造记录](acceptance/console-login-ui.md)。

## 6. 表单

- 表单字段可见标签统一放在控件上方并左对齐。列表查询区在宽屏可横向排列标签与短字段，窄屏恢复标签在上。
- 用户离开已编辑字段时校验该字段，提交时再校验全部字段；不在用户尚未完成输入时逐字符报错。
- 字段错误显示在对应控件附近；表单级或依赖服务错误显示在表单顶部。失败后保留全部已填写内容，并将焦点移动到第一个需要处理的问题或表单级错误摘要。
- 存在未保存修改时，关闭弹窗、返回或切换页面必须先确认是否放弃；MVP 不提供自动草稿。
- 提交期间保存按钮显示进行中状态并阻止重复提交，表单内容保持可见；成功后显示明确结果，失败后允许原地修改和重试。

## 7. 表格

- `ServerTable` 默认保留普通布局；A 样板选择 `presentation="panel"`，查询区与表格区分开，并提供列表标题。没有批量操作的 Tenant 列表使用 `selectable={false}`，不展示无用途的选择框；分页继续使用真实 cursor，不虚构总数。

- 使用明确的服务端分页，不采用无限滚动。
- 填写筛选条件后通过“查询”或 Enter 一次应用；“重置”恢复默认条件并重新查询，不按每个输入字符连续请求。
- 只有服务端明确支持的列显示排序能力；MVP 每次只允许一个排序字段。
- 批量选择只在当前页有效；翻页或改变查询条件时清除选择并明确告知用户，不保留不可见的跨页选择。
- 操作列固定在右侧，窄屏横向滚动时仍保持可见。每行最多占用三个操作位置：操作总数不超过三个时直接显示；超过三个时直接显示前两个，第三个位置显示“更多”，危险操作与普通操作在菜单中明确分隔。
- 首次加载显示表格加载状态；重新查询时保留旧内容并标记正在更新。
- 无任何数据、筛选无结果和加载失败使用不同状态。筛选无结果可以重置条件；加载失败保留查询条件并允许重试。

## 8. 反馈、空态、加载态与错误态

- 成功操作同时更新当前内容并显示短暂、非阻塞的成功反馈。
- 需要用户处理的失败信息持续显示在相关区域，直到问题解决或用户主动关闭，不能只依赖自动消失的消息。
- 空状态使用简洁图标、明确原因和至多一个合理的下一步操作，不使用大型装饰插画。
- 页面或表格首次加载显示内容轮廓；按钮提交在按钮内显示进行中状态；局部刷新只标记对应区域。
- 除应用首次启动外，不使用全屏加载层阻断其他仍可使用的功能。

## 9. 危险操作

- 普通保存不确认。
- 停用、撤销和可恢复删除使用确认弹窗，明确显示操作对象和后果。
- 永久删除或大范围撤销等不可恢复操作还必须输入对象名称或指定文字。
- 危险确认的默认焦点位于取消操作；按 Enter 不能直接执行危险动作；Esc 只代表取消。
- 操作完成后，对象仍存在时焦点返回原触发按钮；对象被移除时依次移动到下一行、表格标题或空状态，不能丢失到文档起点。

## 10. 键盘与焦点

- 使用 Tab 导航时，当前交互元素显示清晰的主色焦点环；鼠标点击时不强制显示。
- Tab 顺序与页面从上到下、从左到右的视觉和文档顺序一致，消费者不得通过自定义顺序制造跳转。
- Esc 只关闭最上层菜单、提示或弹窗并恢复触发点焦点；有未保存内容时先进入放弃确认。
- 查询条件中的 Enter 执行查询；普通按钮支持 Enter 与空格；多行文本框中的 Enter 只输入换行；危险操作不能由 Enter 直接执行。
- MVP 不增加自定义全局快捷键，只支持浏览器和可访问组件的通用键盘语义。
- 路由切换后焦点移动到新页面主标题，并通过可访问页面状态通知读屏软件。

## 11. 组件范围与展示册

Design System 先维护完整公共能力清单，再随真实产品页面逐步实现，不提前制作没有消费者的高级组件。第一批至少包括：

- 主题、Token、图标、按钮和链接；
- 输入框、密码框、选择框、复选框、字段错误和表单错误汇总；
- 普通弹窗与危险确认弹窗；
- 成功提示、警告和持续错误提示；
- 加载、空数据、失败和 404；
- 页面标题和标准页面布局；
- 全宽管理页面、响应式内容栅格和主内容与辅助栏分栏；
- 表格、查询条件、分页、“更多”菜单和文字提示。

图表、富文本和拖拽设计器等高级能力只在出现已确认的真实需求后评审。仓库建立私有组件展示册，集中呈现组件的中英文、浅色、深色和关键交互状态，并作为使用说明、视觉快照和回归入口；具体展示工具在正式实现首批组件时确定。

## 12. 组件基础选型

底层组件基础确定为 **Ant Design 6.6.2**。它只能由 `@saas-forge/design-system` 内部依赖和封装，Platform Console、Tenant Console Shell 与 Remote 不得直接导入 `antd`、引用 Ant Design 全局样式或覆盖其内部选择器。

消费者不得在 `dependencies`、`devDependencies`、`peerDependencies` 或 `optionalDependencies` 中声明 `antd`，不得重复安装 Theme Provider、重写受控 Token 或覆盖 Design System 内部选择器。领域页面仍可使用局部作用域样式排列领域内容，但不能改变公共组件的外观、行为、状态语义或无障碍约束。

Design System 使用 Ant Design 的 `ConfigProvider`、主题算法和 Design Token 作为底层能力，再通过本规范定义的语义 Token 与公共组件形成唯一公开接口。业务消费者只认识 SaaS Forge 的组件名称、属性和状态语义，因此后续替换底层组件库时，迁移范围应被限制在 Design System 内部。

选型前在不修改正式页面的隔离位置制作了 Ant Design 6.6.2 与 React Aria Components 1.20.0 的同场景原型，包括：

- 带服务端分页、查询、单列排序、当前页选择、固定操作列和“更多”菜单的数据表格；
- 标签上方左对齐、离开字段校验、错误汇总、提交状态和未保存保护的表单；
- 可恢复与不可恢复危险操作确认，覆盖默认取消、输入确认、Esc、Enter 和焦点恢复。

选型时使用以下顺序：

1. 键盘、焦点或读屏存在无法接受的问题时直接淘汰；
2. 比较表格、表单和弹窗对本规范的覆盖程度；
3. 比较实现复杂度、维护活跃度、升级风险和最终文件大小；
4. 使用同一套 SaaS Forge 视觉规则比较，不按候选默认外观决定；
5. 向用户展示证据并由用户确认最终依赖。

原型的生产构建将两个候选拆分为独立代码块；在同一原型中，Ant Design 候选压缩后约 `233.19 kB`，React Aria Components 候选压缩后约 `90.22 kB`。这只是原型体积，不代表正式 Design System 的最终大小。Ant Design 提供了更完整的后台表格、表单、反馈和弹窗组合能力，代价是更大的初始文件与更严格的封装、按需加载和升级回归要求。用户已确认选择 Ant Design 6.6.2。

MVP 固定使用 Ant Design 6.6.2。后续升级必须作为 Design System 内的独立受控工作统一进行，评审上游变更，并通过共享组件、键盘、焦点、无障碍、视觉、两个 Console 与代表性 Remote 的完整回归后同步更新本规范；消费者不得自行升级或形成多版本共存。

原型还验证出 Ant Design 表格的分页与通用 `onChange` 会同时触发；封装层必须根据变更类型区分分页和排序，防止翻页被错误重置。该行为需要固定为自动化回归测试。原型不算正式产品功能，不勾选 MVP 完成项。

## 13. 验证与支持范围

- 当前开发阶段仅支持桌面 Google Chrome 当前稳定版；不承诺旧版或移动端 Chrome、Edge、Firefox、Safari、其他 Chromium 浏览器或 Internet Explorer 的兼容性。Chromium 仅作为日常功能与视觉测试工具，首次对外发布前按 [ADR 0046](adr/0046-development-supports-chrome-and-jdk17.md) 重新评估支持范围。
- 基础无障碍以 WCAG 2.2 AA 作为工程验收基线，覆盖可访问名称、角色、状态、文档与 Tab 顺序、可见焦点、错误关联、实时通知、颜色对比度，以及缩放后的可读与可操作。自动检查必须补充真实键盘走查；满足该工程基线不等同于经过正式无障碍认证。
- 在相当于 `320 CSS px` 的可用宽度下，页面主体必须保持单向纵向阅读和操作，不得出现页面级横向滚动；数据表格等确实依赖二维关系的内容可以在自身区域受控横向滚动。聚焦元素必须保持在可见区域，布局组件不得引入额外键盘模式。
- 每次修改公共组件必须自动检查组件功能、键盘操作、基础无障碍、中英文、浅色/深色和关键稳定状态视觉快照。
- 新布局能力必须以增量公共 API 交付，保持 `PageLayout` 默认外观、`FormRow` 和现有消费者行为不变；不得自动迁移现有页面，真实页面只能显式采用新能力。
- 布局单元测试覆盖默认值、公开属性、语义结构和辅助栏可访问名称；真实浏览器验证两种栅格意图、分栏切换、DOM 与 Tab 顺序、可见焦点、局部表格滚动和页面无横向溢出，并覆盖既有五个固定视口与相当于 `320 CSS px` 的可用宽度。Chromium 执行日常功能与稳定边界状态的视觉快照检查，真实 Chrome 承担必要的产品验收。
- 必须验证 Platform Console、Tenant Console Shell 与代表性 Remote 使用同一版本、同一主题入口和同一公共交互。
- 静态门禁自动发现当前及后续新增的 Console 与官方 Remote，禁止消费者在任何依赖分区声明底层组件库，或导入底层组件库、内部 Design System 文件、全局 CSS、重复 Theme Provider，以及覆盖底层组件或公共组件的内部选择器。
- 创建共享包、组件能渲染或单个应用测试通过都不足以完成 MVP；只有共享消费、边界门禁和相应浏览器证据全部成立，才能勾选 [MVP 开发计划](16-mvp-development-plan.md)中的 Design System 事项。
- “响应式栅格和标准分栏布局”事项只有在公共布局从 Design System 根入口提供、组件与真实浏览器验证通过、Platform Console 与 Tenant Console Shell 的最终产品路由各有一个真实消费场景、代表性 Remote 能正确消费，并且对应核心流程在 `390px` 与 `360px` 均可完成后才能勾选。展示入口、消费夹具或一次性验收页面不能替代两个 Console 的最终产品证据；如果真实页面尚未出现，可以先交付公共能力，但该事项继续保持未完成。
