# Issue #191 验证记录

## 实现

- 分支：`codex/issue-191`；提交：`3967013`。
- 固定上游：Soybean Admin Element Plus `7613bd206cd42001b40e3eafceeb895dcbc277a8`。
- 平台登录、后台布局、导航、标签栏、首页接入；旧业务路由保留；认证使用单 PLATFORM Runtime。
- 来源与允许差异：`consoles/platform-console/src/vendor/soybean/UPSTREAM.md`。
- 平台首次改密保留。密码设置遵循现有 TENANT-only 契约；误入平台的 challenge 立即从 URL 清除，无提交或转发。

## 自动验证

- 工作区 typecheck、lint、format:check、validate:i18n、build:workspace 通过。
- 完整前端 test 已执行：边界 104、i18n 15、Runtime 249、admin 30 项通过；平台旧业务回归最初因测试 HMR 配置污染失败，修复测试服务器配置后，平台包 15 项通过。
- 随后补充认证失败焦点/策略提示及误入密码设置回归，桌面 Chrome 的平台 4 项通过。包括实际正式路由 + 模拟 HTTP 的登录、刷新、退出、首次改密、失败重试、两种主题、两种语言和 WCAG 自动检查。
- 平台双标签协调与跨 Origin Locale 隔离通过；租户双标签同组首次出现 refresh=2（期望1），单独复跑通过。保留该偶发现象，不认定稳定性问题已解决。
- 双轴代码审查已完成，发现的恢复错误码遗漏、失败焦点和密码策略提示问题均已修复并复核。
- 模拟截图位于 `/tmp/issue191-evidence/`；这些截图不证明真实后端行为。

## 真实环境（2026-09-15）

- 用户自行将平台前端切换至当前 worktree；未接管其后端或 Gateway 生命周期。
- HTTPS `/src/main.ts` 与本机 Vite 均返回200，确认返回本次 `App.vue` 入口，旧 mountConsole 未挂载。此项仅为代码来源证明。
- 原 Chrome 标签页出现 ERR_BLOCKED_BY_CLIENT；同一 Chrome 新标签页正常，无需调整安全设置，原标签页拦截原因未确认。
- 新标签页 `https://platform.saas.forge.test/login` 已显示正式登录界面，波浪背景、品牌区、卡片、邮箱/密码输入、语言和主题控件正常；本轮对话已有空密码登录页截图。
- 用户自行完成真实登录，本轮 Chrome 确认进入 `/`，显示当前会话、平台管理员授权和业务快捷入口；不记录邮箱或 Identity 值。
- 刷新后仍显示首页、当前会话和退出按钮。点击退出后进入 `/login`；再次刷新仍显示登录按钮，首页会话内容不再显示。真实首页视口为 1800×953，scrollWidth=1800。
- 本轮未触发真实首次改密；首次改密、失败重试及密码设置边界只具备模拟 HTTP 回归证据。
- 下载并运行固定提交的完整上游应用，临时目录 `/tmp/issue191-soybean`，独立端口 19527。上游仅添加本地视觉用认证响应夹具，以进入原始首页；该夹具不构成产品认证证据。
- 本轮浏览器已展示固定版本登录页、首页及平台登录页截图。登录页逐页实测 CSS 视口均为 1440×900，scrollWidth=1440；浏览器缩放造成截图位图尺寸差异，因此按 CSS 尺寸比较，不作逐像素一致声明。
- 登录页保留官方波浪背景、居中卡片、品牌区、表单和圆角主按钮。允许差异：品牌与主色替换、语言控件增加可见文字、移除演示账号/注册/验证码入口导致卡片高度缩短、增加键盘焦点样式。
- 官方首页已实际展示垂直导航、顶栏、标签栏、欢迎卡片和内容卡片；平台真实首页确认同类结构，内容改为真实会话与业务入口。首页没有完成同尺寸截图配对，不能声明首页像素级对齐。
- 临时视口覆盖已恢复；临时比较标签页已关闭。用户的产品登录页保留。

## 完成边界

本地实现、模拟回归及真实登录/恢复/退出已完成；真实首次改密与首页同尺寸截图配对未完成，不声明完整验收通过；未推送、未执行当前提交远端 CI、未关闭 Issue #191。
