# Issue #202 统一认证协议设计验收

日期：2026-09-15。

结论：**协议准备完成，业务实现未执行。** 用户逐轮确认 Q1–Q10，并最终确认完整方案，包括显式版本化、可恢复整体退出及该环境旧协议退役影响。此次确认不等于执行停用或发布的指令。

## 交付物与验收项

主文档：[统一认证协议与兼容迁移方案](../unified-authentication-protocol.md)。决策：[ADR 0052](../adr/0052-unified-console-authentication-uses-versioned-session-protocol.md)。

| #202 验收标准 | 对应交付与结论 |
|---|---|
| 平台、租户、双身份、无上下文、初始凭据状态；不重复认证 | 协议第 2、4 节：状态矩阵、分组卡片、一次凭据认证、互斥公开结果与首次改密限制 |
| 操作、错误、Token/Family/Cookie 和跨标签协调 | 第 3–6 节：v2 operation、版本/幂等恢复、单一 Family、Slot 定位、刷新轮换、全部 JTI 退出、Cookie 晚到降级与脏表单保护 |
| 兼容、旧会话、发布顺序、回退与不可变基线 | 第 7 节：v2 与旧协议显式退役，旧 Cookie/Token 拒绝、受控切换、只回退兼容组合；不扩展 v1 例外，不修改历史 SQL |
| 公开负例与后端授权/前端显示区别 | 第 8 节：越权、失效、并发刷新、旧响应、换账号、受限凭据及原操作者案例；明确这些为后续验收要求，未伪称已执行 |
| ADR、领域术语、接口与迁移设计 | ADR 0052；0026/0027/0031/0038/0039/0051 的新旧适用边界；IAM/Gateway 词汇同步；第 4、7 节给出接口和迁移步骤 |
| 正式制品兼容与未实现标记 | 本次只修改文档，无正式契约、生成代码、运行代码、历史基线或 Flyway 迁移变化；当前消费者不受本次提交影响 |

## 本次验证

- **通过**：`git diff --check`；本次改动 Markdown 的本地文件链接存在性与 JSON 示例语法检查。
- **通过**：逐项核对 Q1–Q10、Issue 验收标准及公开案例；复核并修正幂等重试与版本前置条件的顺序、204 返回版本、已提交 Cookie 响应乱序及持久转换中的读取阻断。
- **通过**：变更路径检查，确认仅协议、ADR、领域词汇和本记录发生变化。
- **未执行**：Maven、正式契约兼容测试、数据库迁移测试、Chrome/服务集成和运行时安全验证。此次未改正式制品或运行行为，不以空跑业务检查冒充新协议已实现。
- **未执行**：制品发布、Nacos 变更、部署、旧协议停用及会话撤销。

## 后续实现边界

后续任务必须将 v2 契约纳入正式 OpenAPI、route catalog 和固定版本 Client；实现统一 Slot/Family、可恢复撤销、新旧协议退役和仅前向迁移，并执行协议第 8 节的后端与真实 Chrome 验收。发布前核实实际环境配置、消费者、版本组合和回退能力；外部旧消费者是否存在仍未知，不能因本票关闭推断不存在。

普通退出/权限失效只终止当前浏览器会话；旧协议正式退役则要求该环境所有仍使用旧版的设备进入新版重新登录。没有兼容的回退组合时进入维护，不自动重新开放旧登录，不复活旧会话。
