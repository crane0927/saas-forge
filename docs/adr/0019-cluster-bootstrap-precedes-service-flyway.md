# 集群引导先于服务 Flyway 迁移

受控的集群引导工件是唯一可使用 PostgreSQL 管理账号的路径，负责创建四个逻辑数据库、每库的迁移与运行时账号及最小授权；本地 Compose 与生产数据库运行方执行等价流程。各领域服务的 Flyway 仅以所属 `*_migrator` 连接既有数据库并管理本服务业务对象，运行时只使用 `*_app`。这将集群级权限与领域迁移责任分离，避免任何服务获得跨库管理能力。
