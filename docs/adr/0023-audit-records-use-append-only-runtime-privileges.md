# 审计记录以运行时最小权限实现只追加

`audit_app` 对 `audit_records` 仅被授予 `SELECT` 与 `INSERT`，没有 `UPDATE`、`DELETE`、`TRUNCATE`，也不使用软删除；可变的 `export_jobs` 则单独按任务语义授予权限。迁移账号保留受控架构演进能力，但既有迁移不可改写。该选择让常驻审计服务的数据库权限直接表达只追加语义，避免以无法约束高权限迁移账号的触发器制造伪防篡改保证。
