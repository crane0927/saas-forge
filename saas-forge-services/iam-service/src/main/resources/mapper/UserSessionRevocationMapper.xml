<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.saasforge.iam.infrastructure.persistence.mapper.UserSessionRevocationMapper">
    <resultMap id="workflowRow" type="io.saasforge.iam.infrastructure.persistence.record.UserSessionRevocationRow">
        <id property="revocationRequestId" column="revocation_request_id"/>
        <result property="targetType" column="target_type"/>
        <result property="tenantId" column="tenant_id"/>
        <result property="membershipId" column="membership_id"/>
        <result property="revocationStatus" column="revocation_status"/>
        <result property="cursorFamilyId" column="cursor_family_id"/>
        <result property="revokedFamilyCount" column="revoked_family_count"/>
        <result property="revokedJtiCount" column="revoked_jti_count"/>
        <result property="attemptCount" column="attempt_count"/>
        <result property="nextAttemptAt" column="next_attempt_at"/>
        <result property="leaseOwner" column="lease_owner"/>
        <result property="leaseUntil" column="lease_until"/>
        <result property="fencingToken" column="fencing_token"/>
        <result property="recoveryExhaustedAt" column="recovery_exhausted_at"/>
        <result property="lastFailure" column="last_failure"/>
        <result property="completedAt" column="completed_at"/>
    </resultMap>

    <resultMap id="candidateRow" type="io.saasforge.iam.infrastructure.persistence.record.UserSessionRevocationCandidateRow">
        <id property="familyId" column="family_id"/>
        <result property="revokeFamily" column="revoke_family"/>
        <result property="jti" column="jti"/>
        <result property="identityId" column="identity_id"/>
        <result property="membershipId" column="membership_id"/>
        <result property="tenantId" column="tenant_id"/>
        <result property="kid" column="kid"/>
        <result property="issuedAt" column="issued_at"/>
        <result property="expiresAt" column="expires_at"/>
    </resultMap>

    <sql id="workflowColumns">
        w.revocation_request_id, f.target_type, f.tenant_id, f.membership_id,
        w.revocation_status, w.cursor_family_id, w.revoked_family_count, w.revoked_jti_count,
        w.attempt_count, w.next_attempt_at, w.lease_owner, w.lease_until, w.fencing_token,
        w.recovery_exhausted_at, w.last_failure, w.completed_at
    </sql>

    <select id="find" resultMap="workflowRow">
        SELECT <include refid="workflowColumns"/>
        FROM iam_user_session_revocations w
        JOIN iam_revocation_fences f USING (revocation_request_id)
        WHERE w.revocation_request_id = #{requestId}
    </select>

    <insert id="insert">
        INSERT INTO iam_user_session_revocations (revocation_request_id, revocation_status, next_attempt_at)
        VALUES (#{requestId}, 'PENDING', #{at})
        ON CONFLICT (revocation_request_id) DO NOTHING
    </insert>

    <select id="claim" resultMap="workflowRow" affectData="true" flushCache="true">
        WITH claimed AS (
            UPDATE iam_user_session_revocations
            SET lease_owner = #{claimant}, lease_until = #{leaseUntil},
                attempt_count = attempt_count + 1, fencing_token = fencing_token + 1
            WHERE revocation_request_id = #{requestId}
              AND revocation_status = 'PENDING' AND attempt_count &lt; #{maximumAttempts}
              AND next_attempt_at &lt;= #{now} AND (lease_until IS NULL OR lease_until &lt;= #{now})
            RETURNING revocation_request_id
        )
        SELECT <include refid="workflowColumns"/>
        FROM claimed c
        JOIN iam_user_session_revocations w USING (revocation_request_id)
        JOIN iam_revocation_fences f USING (revocation_request_id)
    </select>

    <select id="claimNext" resultMap="workflowRow" affectData="true" flushCache="true">
        WITH candidate AS (
            SELECT revocation_request_id
            FROM iam_user_session_revocations
            WHERE revocation_status = 'PENDING' AND attempt_count &lt; #{maximumAttempts}
              AND next_attempt_at &lt;= #{now} AND (lease_until IS NULL OR lease_until &lt;= #{now})
            ORDER BY next_attempt_at, revocation_request_id
            FOR UPDATE SKIP LOCKED LIMIT 1
        ), claimed AS (
            UPDATE iam_user_session_revocations w
            SET lease_owner = #{claimant}, lease_until = #{leaseUntil},
                attempt_count = w.attempt_count + 1, fencing_token = w.fencing_token + 1
            FROM candidate c WHERE w.revocation_request_id = c.revocation_request_id
            RETURNING w.revocation_request_id
        )
        SELECT <include refid="workflowColumns"/>
        FROM claimed c
        JOIN iam_user_session_revocations w USING (revocation_request_id)
        JOIN iam_revocation_fences f USING (revocation_request_id)
    </select>

    <update id="exhaustExpiredAtLimit">
        UPDATE iam_user_session_revocations
        SET revocation_status = 'RECOVERY_REQUIRED', recovery_exhausted_at = #{now},
            lease_owner = NULL, lease_until = NULL, last_failure = #{failure}
        WHERE revocation_status = 'PENDING' AND attempt_count &gt;= #{maximumAttempts}
          AND (lease_until IS NULL OR lease_until &lt;= #{now})
    </update>

    <select id="findCandidates" resultMap="candidateRow">
        WITH candidates AS (
            SELECT f.id AS family_id,
                   (f.revoked_at IS NULL AND f.absolute_expires_at &gt; #{at}
                    AND ((#{targetType} = 'TENANT' AND f.tenant_id = #{tenantId})
                      OR (#{targetType} = 'MEMBERSHIP' AND f.membership_id = #{membershipId}))) AS revoke_family
            FROM iam_refresh_token_families f
            WHERE f.family_purpose = 'USER_TENANT'
              AND (#{cursor}::uuid IS NULL OR f.id &gt; #{cursor})
              AND (
                (f.revoked_at IS NULL AND f.absolute_expires_at &gt; #{at}
                 AND ((#{targetType} = 'TENANT' AND f.tenant_id = #{tenantId})
                   OR (#{targetType} = 'MEMBERSHIP' AND f.membership_id = #{membershipId})))
                OR EXISTS (
                    SELECT 1 FROM iam_access_token_issuances x
                    WHERE x.family_id = f.id AND x.revoked_at IS NULL
                      AND x.expires_at + INTERVAL '30 seconds' &gt; #{at}
                      AND ((#{targetType} = 'TENANT' AND x.tenant_id = #{tenantId})
                        OR (#{targetType} = 'MEMBERSHIP' AND x.membership_id = #{membershipId}))
                )
              )
            ORDER BY f.id LIMIT #{batchSize}
        )
        SELECT c.family_id, c.revoke_family, i.jti, i.identity_id, i.membership_id, i.tenant_id,
               i.kid, i.issued_at, i.expires_at
        FROM candidates c
        LEFT JOIN iam_access_token_issuances i ON i.family_id = c.family_id
          AND i.revoked_at IS NULL AND i.expires_at + INTERVAL '30 seconds' &gt; #{at}
          AND (c.revoke_family
            OR (#{targetType} = 'TENANT' AND i.tenant_id = #{tenantId})
            OR (#{targetType} = 'MEMBERSHIP' AND i.membership_id = #{membershipId}))
        ORDER BY c.family_id, i.jti
    </select>

    <update id="revokeFamilies">
        UPDATE iam_refresh_token_families SET revoked_at = GREATEST(#{at}, last_used_at)
        WHERE revoked_at IS NULL AND absolute_expires_at &gt; #{at} AND id IN
        <foreach collection="familyIds" item="id" open="(" separator="," close=")">#{id}</foreach>
    </update>

    <update id="revokeIssuances">
        UPDATE iam_access_token_issuances
        SET revoked_at = #{at}, revocation_reason = 'TARGET_SESSION_REVOCATION'
        WHERE revoked_at IS NULL AND expires_at + INTERVAL '30 seconds' &gt; #{at} AND jti IN
        <foreach collection="jtis" item="jti" open="(" separator="," close=")">#{jti}</foreach>
    </update>

    <update id="advance">
        UPDATE iam_user_session_revocations
        SET cursor_family_id = COALESCE(#{cursor}, cursor_family_id),
            revoked_family_count = revoked_family_count + #{familyCount},
            revoked_jti_count = revoked_jti_count + #{jtiCount},
            revocation_status = CASE WHEN #{completed} THEN 'COMPLETED' ELSE 'PENDING' END,
            completed_at = CASE WHEN #{completed} THEN #{at} ELSE NULL END,
            attempt_count = 0, next_attempt_at = #{at}, lease_owner = NULL, lease_until = NULL,
            last_failure = NULL
        WHERE revocation_request_id = #{requestId} AND revocation_status = 'PENDING'
          AND fencing_token = #{fencingToken}
    </update>

    <update id="scheduleRetry">
        UPDATE iam_user_session_revocations
        SET next_attempt_at = #{retryAt}, lease_owner = NULL, lease_until = NULL, last_failure = #{failure}
        WHERE revocation_request_id = #{requestId} AND revocation_status = 'PENDING'
          AND fencing_token = #{fencingToken}
    </update>

    <update id="exhaust">
        UPDATE iam_user_session_revocations
        SET revocation_status = 'RECOVERY_REQUIRED', recovery_exhausted_at = #{at},
            lease_owner = NULL, lease_until = NULL, last_failure = #{failure}
        WHERE revocation_request_id = #{requestId} AND revocation_status = 'PENDING'
          AND fencing_token = #{fencingToken}
    </update>

    <update id="recover">
        UPDATE iam_user_session_revocations
        SET revocation_status = 'PENDING', recovery_exhausted_at = NULL, last_failure = NULL,
            attempt_count = 0, next_attempt_at = #{at}, lease_owner = NULL, lease_until = NULL
        WHERE revocation_request_id = #{requestId} AND revocation_status = 'RECOVERY_REQUIRED'
    </update>

    <select id="findRelease" resultMap="workflowRow">
        SELECT <include refid="workflowColumns"/>
        FROM iam_user_session_fence_releases r
        JOIN iam_user_session_revocations w USING (revocation_request_id)
        JOIN iam_revocation_fences f USING (revocation_request_id)
        WHERE r.release_request_id = #{releaseRequestId}
    </select>

    <insert id="insertRelease">
        INSERT INTO iam_user_session_fence_releases
            (release_request_id, revocation_request_id, target_type, target_id, released_at)
        VALUES (#{releaseRequestId}, #{revocationRequestId}, #{targetType}, #{targetId}, #{at})
    </insert>
</mapper>
