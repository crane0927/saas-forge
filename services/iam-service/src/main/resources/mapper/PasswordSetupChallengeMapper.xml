<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.saasforge.iam.infrastructure.persistence.mapper.PasswordSetupChallengeMapper">
    <resultMap id="challengeRow" type="io.saasforge.iam.infrastructure.persistence.record.PasswordSetupChallengeRow">
        <id property="id" column="id"/>
        <result property="identityId" column="identity_id"/>
        <result property="tokenDigest" column="token_digest"/>
        <result property="issuedAt" column="issued_at"/>
        <result property="expiresAt" column="expires_at"/>
        <result property="invalidatedAt" column="invalidated_at"/>
        <result property="consumedAt" column="consumed_at"/>
        <result property="idempotencyKey" column="idempotency_key"/>
        <result property="requestFingerprint" column="request_fingerprint"/>
        <result property="credentialId" column="credential_id"/>
        <result property="completedStatus" column="completed_status"/>
    </resultMap>

    <select id="lockEligibleIdentity" resultType="java.util.UUID">
        SELECT identity.id
        FROM iam_identities identity
        WHERE identity.id = #{identityId}
          AND NOT EXISTS (SELECT 1 FROM iam_credentials WHERE identity_id = identity.id)
        FOR UPDATE
    </select>

    <update id="invalidateOpenChallenges">
        UPDATE iam_password_setup_challenges
        SET invalidated_at = #{invalidatedAt}
        WHERE identity_id = #{identityId} AND invalidated_at IS NULL AND consumed_at IS NULL
    </update>

    <select id="insertChallenge" resultMap="challengeRow" affectData="true" flushCache="true">
        INSERT INTO iam_password_setup_challenges (identity_id, token_digest, issued_at, expires_at)
        VALUES (#{row.identityId}, #{row.tokenDigest}, #{row.issuedAt}, #{row.expiresAt})
        RETURNING id, identity_id, token_digest, issued_at, expires_at, invalidated_at, consumed_at,
                  idempotency_key, request_fingerprint, credential_id, completed_status
    </select>

    <sql id="challengeColumns">
        id, identity_id, token_digest, issued_at, expires_at, invalidated_at, consumed_at,
        idempotency_key, request_fingerprint, credential_id, completed_status
    </sql>

    <select id="findByTokenDigest" resultMap="challengeRow">
        SELECT <include refid="challengeColumns"/>
        FROM iam_password_setup_challenges
        WHERE token_digest = #{tokenDigest}
    </select>

    <select id="lockByTokenDigest" resultMap="challengeRow">
        SELECT <include refid="challengeColumns"/>
        FROM iam_password_setup_challenges
        WHERE token_digest = #{tokenDigest}
        FOR UPDATE
    </select>

    <update id="complete">
        UPDATE iam_password_setup_challenges
        SET consumed_at = #{consumedAt}, idempotency_key = #{idempotencyKey},
            request_fingerprint = #{requestFingerprint}, credential_id = #{credentialId}, completed_status = 204
        WHERE id = #{challengeId} AND invalidated_at IS NULL AND consumed_at IS NULL
          AND expires_at > #{consumedAt}
    </update>
</mapper>
